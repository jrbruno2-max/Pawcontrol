
<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0">
<title>Método Praxis</title>
<meta name="application-name" content="Método Praxis">
<meta name="apple-mobile-web-app-title" content="Praxis">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="default">
<meta name="theme-color" content="#7B5EA7">
<link rel="icon" type="image/svg+xml" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 64 64'%3E%3Crect width='64' height='64' rx='14' fill='%23F5F0EA'/%3E%3Ccircle cx='25' cy='30' r='16' fill='%237B5EA7' opacity='.9'/%3E%3Ccircle cx='39' cy='30' r='16' fill='%23F0A030' opacity='.8'/%3E%3Ccircle cx='32' cy='40' r='13' fill='%23D94F7E' opacity='.85'/%3E%3Ccircle cx='32' cy='20' r='7' fill='%234F8FD4' opacity='.75'/%3E%3C/svg%3E">
<link rel="apple-touch-icon" href="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 180 180'%3E%3Crect width='180' height='180' rx='40' fill='%23F5F0EA'/%3E%3Ccircle cx='70' cy='85' r='45' fill='%237B5EA7' opacity='.9'/%3E%3Ccircle cx='110' cy='85' r='45' fill='%23F0A030' opacity='.8'/%3E%3Ccircle cx='90' cy='110' r='37' fill='%23D94F7E' opacity='.85'/%3E%3Ccircle cx='90' cy='56' r='20' fill='%234F8FD4' opacity='.75'/%3E%3C/svg%3E">
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#F5F0EA;--sf:#FFFFFF;--s2:#EDE8E0;--bd:#DDD6CC;
  --pu:#7B5EA7;--or:#D4891A;--pk:#C84070;
  --tx:#1C1A18;--mu:#7A7470;--dm:#B0A898;
  --gn:#2E8B57;--rd:#C0392B;--co:#2980B9;--te:#1A9E8A;
  --sha:0 1px 4px rgba(0,0,0,.08);--sha2:0 2px 12px rgba(0,0,0,.12)
}
html,body{height:100%;background:var(--bg);color:var(--tx);font-family:'Plus Jakarta Sans',sans-serif;font-size:14px;-webkit-tap-highlight-color:transparent}
.app{max-width:480px;margin:0 auto;min-height:100vh}
/* HEADER */
.hdr{background:var(--sf);border-bottom:1px solid var(--bd);padding:11px 16px 9px;position:sticky;top:0;z-index:50;box-shadow:var(--sha)}
.htop{display:flex;align-items:center;justify-content:space-between;margin-bottom:9px}
.brand{display:flex;align-items:center;gap:8px}
.bico{width:34px;height:34px;border-radius:9px;background:var(--s2);display:flex;align-items:center;justify-content:center;flex-shrink:0}
.bico svg{width:34px;height:34px}
.bnm{font-size:.9rem;font-weight:800;background:linear-gradient(90deg,var(--pu),var(--pk));-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text}
.bsb{font-size:.52rem;color:var(--mu)}
.obadge{display:flex;align-items:center;gap:4px;background:rgba(46,139,87,.1);border:1px solid rgba(46,139,87,.25);border-radius:20px;padding:3px 8px;font-size:.6rem;font-weight:700;color:var(--gn)}
.odot{width:5px;height:5px;background:var(--gn);border-radius:50%;animation:pulse 2s infinite}
@keyframes pulse{0%,100%{opacity:1}50%{opacity:.4}}
.swp{position:relative}
.sic{position:absolute;left:11px;top:50%;transform:translateY(-50%);font-size:13px;pointer-events:none;color:var(--mu)}
.sinp{width:100%;background:var(--bg);border:1.5px solid var(--bd);border-radius:9px;padding:8px 34px 8px 36px;font-family:inherit;font-size:.82rem;color:var(--tx);outline:none;transition:border-color .2s}
.sinp::placeholder{color:var(--dm)}.sinp:focus{border-color:var(--pu)}
.scl{position:absolute;right:9px;top:50%;transform:translateY(-50%);background:var(--bd);border:none;border-radius:50%;width:19px;height:19px;display:none;align-items:center;justify-content:center;cursor:pointer;font-size:9px;color:var(--mu)}
.scl.v{display:flex}
/* PAGE */
.ph{padding:13px 16px 0;display:flex;align-items:flex-start;justify-content:space-between}
.pt{font-size:1.3rem;font-weight:800;letter-spacing:-.02em}
.ps{font-size:.67rem;color:var(--mu);margin-top:1px}
.bno{display:flex;align-items:center;gap:4px;background:linear-gradient(135deg,var(--pu),var(--pk));color:#fff;border:none;border-radius:9px;padding:7px 11px;font-family:inherit;font-size:.72rem;font-weight:700;cursor:pointer;white-space:nowrap;box-shadow:0 2px 8px rgba(123,94,167,.3)}
.bno:active{opacity:.8}
.flt{padding:9px 16px;display:flex;gap:6px;overflow-x:auto;scrollbar-width:none}
.flt::-webkit-scrollbar{display:none}
.chip{flex-shrink:0;padding:4px 10px;border-radius:20px;font-size:.67rem;font-weight:600;border:1.5px solid var(--bd);background:var(--sf);color:var(--mu);cursor:pointer;font-family:inherit}
.chip.active{background:var(--pu);border-color:var(--pu);color:#fff}
.rc{padding:0 16px 5px;font-size:.67rem;color:var(--mu)}.rc strong{color:var(--pu)}
.lst{padding:0 16px 105px}
/* CARD */
.card{display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--sf);border-radius:11px;margin-bottom:6px;border:1px solid var(--bd);cursor:pointer;transition:box-shadow .15s,border-color .15s;animation:sIn .2s ease both;box-shadow:var(--sha)}
.card:hover{box-shadow:var(--sha2);border-color:rgba(123,94,167,.3)}
.card:active{opacity:.85}
@keyframes sIn{from{opacity:0;transform:translateY(5px)}to{opacity:1;transform:translateY(0)}}
.av{width:38px;height:38px;border-radius:9px;display:flex;align-items:center;justify-content:center;font-size:18px;flex-shrink:0}
.ci{flex:1;min-width:0}.cnr{display:flex;align-items:baseline;gap:5px;flex-wrap:wrap}
.cn{font-size:.88rem;font-weight:700;color:var(--tx)}.cs{font-size:.63rem;color:var(--mu)}
.cm{font-size:.68rem;color:var(--mu);margin-top:1px}.cm strong{color:var(--tx);font-weight:600}
.cr{display:flex;flex-direction:column;align-items:flex-end;gap:4px;flex-shrink:0}
.badge{font-size:.57rem;font-weight:700;letter-spacing:.04em;text-transform:uppercase;padding:2px 6px;border-radius:4px}
.badge.ativo,.badge.ativa{background:rgba(46,139,87,.12);color:var(--gn)}
.badge.concluido,.badge.concluida{background:rgba(41,128,185,.12);color:var(--co)}
.badge.avaliacao{background:rgba(212,137,26,.12);color:var(--or)}
.badge.agendado{background:rgba(123,94,167,.12);color:var(--pu)}
.badge.hospedado{background:rgba(26,158,138,.12);color:var(--te)}
.badge.pago{background:rgba(46,139,87,.12);color:var(--gn)}
.badge.pendente{background:rgba(212,137,26,.12);color:var(--or)}
.badge.parcial{background:rgba(41,128,185,.12);color:var(--co)}
.chv{color:var(--dm);font-size:.8rem}
.pm{margin-top:4px}.pmb{height:3px;background:var(--s2);border-radius:2px;overflow:hidden}.pmf{height:100%;border-radius:2px}.pml{font-size:.57rem;color:var(--mu);margin-top:1px}
/* EMPTY */
.empty{text-align:center;padding:45px 16px;display:none}.empty.v{display:block}
.empty-i{font-size:2.2rem;margin-bottom:10px}.empty-t{font-size:.92rem;font-weight:700;margin-bottom:5px}.empty-s{font-size:.75rem;color:var(--mu);line-height:1.5}
/* NAV — 6 itens */
.bnav{position:fixed;bottom:0;left:50%;transform:translateX(-50%);width:100%;max-width:480px;background:var(--sf);border-top:1px solid var(--bd);display:flex;padding:6px 0 12px;z-index:50;box-shadow:0 -2px 10px rgba(0,0,0,.08)}
.ni{flex:1;display:flex;flex-direction:column;align-items:center;gap:1px;cursor:pointer;padding:2px 0;color:var(--dm);font-size:.48rem;font-weight:700;letter-spacing:.02em;text-transform:uppercase;border:none;background:none;font-family:inherit}
.ni.active{color:var(--pu)}.ni-i{font-size:1.05rem}
/* MODAL */
.mo{position:fixed;inset:0;z-index:200;background:rgba(0,0,0,.45);display:none;align-items:flex-end;justify-content:center}
.mo.open{display:flex}
.mdl{width:100%;max-width:480px;background:var(--sf);border-radius:20px 20px 0 0;border-top:2px solid var(--pu);animation:mUp .24s ease;max-height:88vh;display:flex;flex-direction:column;box-shadow:0 -4px 24px rgba(0,0,0,.15)}
@keyframes mUp{from{transform:translateY(100%)}to{transform:translateY(0)}}
.mhd{display:flex;align-items:center;justify-content:space-between;padding:13px 16px;border-bottom:1px solid var(--bd);flex-shrink:0;background:var(--sf);border-radius:20px 20px 0 0}
.mtt{font-size:.92rem;font-weight:800;color:var(--tx)}
.mcl{background:var(--s2);border:none;border-radius:50%;width:26px;height:26px;display:flex;align-items:center;justify-content:center;cursor:pointer;color:var(--mu);font-size:.82rem;font-weight:700}
.mcl:hover{background:var(--bd)}
.mb{padding:15px;overflow-y:auto;flex:1}
.fg{margin-bottom:11px}
.fl{display:block;font-size:.62rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--mu);margin-bottom:5px}
.fi,.fs{width:100%;background:var(--bg);border:1.5px solid var(--bd);border-radius:8px;padding:9px 11px;font-family:inherit;font-size:.86rem;color:var(--tx);outline:none;transition:border-color .2s;appearance:none;-webkit-appearance:none}
.fi:focus,.fs:focus{border-color:var(--pu)}.fi::placeholder{color:var(--dm)}
.fr{display:grid;grid-template-columns:1fr 1fr;gap:9px}
.fr3{display:grid;grid-template-columns:1fr 1fr 1fr;gap:7px}
.bsv{width:100%;background:linear-gradient(135deg,var(--pu),var(--pk));color:#fff;border:none;border-radius:9px;padding:11px;font-family:inherit;font-size:.85rem;font-weight:700;cursor:pointer;margin-top:4px;margin-bottom:10px;box-shadow:0 2px 8px rgba(123,94,167,.3)}
.bsv:active{opacity:.85}
.ssep{border-top:2px dashed var(--bd);margin:13px 0 11px;padding-top:11px}
.slbl{font-size:.7rem;font-weight:700;color:var(--or);margin-bottom:10px}
/* DIA+HORA SLOTS */
.dh-slot{display:flex;align-items:center;gap:7px;background:var(--bg);border-radius:8px;padding:7px 9px;margin-bottom:5px;border:1px solid var(--bd)}
.dh-sel{flex:1;background:var(--sf);border:1px solid var(--bd);border-radius:6px;padding:6px 8px;font-family:inherit;font-size:.8rem;color:var(--tx);outline:none;appearance:none;-webkit-appearance:none}
.dh-sel:focus{border-color:var(--pu)}
.dh-time{width:85px;background:var(--sf);border:1px solid var(--bd);border-radius:6px;padding:6px 8px;font-family:inherit;font-size:.8rem;color:var(--tx);outline:none;flex-shrink:0}
.dh-time:focus{border-color:var(--pu)}
.dh-rm{background:rgba(192,57,43,.1);border:none;border-radius:6px;color:var(--rd);cursor:pointer;font-size:.88rem;padding:5px 7px;flex-shrink:0;font-weight:700}
.btn-add-slot{width:100%;background:transparent;border:1.5px dashed var(--bd);border-radius:8px;padding:8px;font-family:inherit;font-size:.76rem;font-weight:600;color:var(--mu);cursor:pointer;margin-top:3px;transition:border-color .15s,color .15s}
.btn-add-slot:hover{border-color:var(--pu);color:var(--pu)}
/* TOGGLE */
.trow{display:flex;align-items:center;justify-content:space-between;padding:9px 0;border-bottom:1px solid var(--bd)}
.tlb{font-size:.82rem;font-weight:600;color:var(--tx)}.tsb{font-size:.67rem;color:var(--mu);margin-top:1px}
.tg{position:relative;width:42px;height:22px;flex-shrink:0}
.tg input{opacity:0;width:0;height:0}
.tsl{position:absolute;inset:0;background:var(--bd);border-radius:22px;cursor:pointer;transition:.2s}
.tsl::before{content:'';position:absolute;width:16px;height:16px;left:3px;top:3px;background:#fff;border-radius:50%;transition:.2s;box-shadow:0 1px 3px rgba(0,0,0,.2)}
.tg input:checked+.tsl{background:var(--pu)}.tg input:checked+.tsl::before{transform:translateX(20px)}
/* TIPO BTN */
.tgr{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-bottom:3px}
.tpb{padding:10px 7px;border-radius:8px;border:1.5px solid var(--bd);background:var(--bg);font-family:inherit;font-size:.73rem;font-weight:700;color:var(--mu);cursor:pointer;text-align:center;line-height:1.4}
.tpb.sel{background:rgba(123,94,167,.1);border-color:var(--pu);color:var(--pu)}
/* PGTO CHIPS */
.pgto-grid{display:flex;flex-wrap:wrap;gap:6px}
.pgto-chip{padding:6px 11px;border-radius:7px;border:1.5px solid var(--bd);background:var(--bg);font-family:inherit;font-size:.75rem;font-weight:600;color:var(--mu);cursor:pointer}
.pgto-chip.sel{background:rgba(46,139,87,.1);border-color:var(--gn);color:var(--gn)}
/* DETAIL */
.dav{width:58px;height:58px;border-radius:13px;display:flex;align-items:center;justify-content:center;font-size:26px;margin:0 auto 8px}
.dn{text-align:center;font-size:1.12rem;font-weight:800;margin-bottom:2px;color:var(--tx)}
.dm{text-align:center;color:var(--mu);font-size:.75rem;margin-bottom:11px}
.dact{display:flex;gap:7px;margin-bottom:11px}
.bedt{flex:1;background:var(--bg);color:var(--tx);border:1.5px solid var(--bd);border-radius:8px;padding:7px;font-family:inherit;font-size:.72rem;font-weight:600;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:4px}
.bedt:hover{border-color:var(--pu);color:var(--pu)}
.bdel{flex:1;background:rgba(192,57,43,.06);color:var(--rd);border:1.5px solid rgba(192,57,43,.25);border-radius:8px;padding:7px;font-family:inherit;font-size:.72rem;font-weight:600;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:4px}
.bdel:hover{background:rgba(192,57,43,.12)}
.dgd{display:grid;grid-template-columns:1fr 1fr;gap:7px;margin-bottom:10px}
.df{background:var(--bg);border-radius:7px;padding:9px;border:1px solid var(--bd)}
.dfl{font-size:.57rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--mu);margin-bottom:2px}
.dfv{font-size:.8rem;font-weight:600;color:var(--tx)}
.pbox{background:linear-gradient(135deg,rgba(123,94,167,.08),rgba(200,64,112,.05));border:1px solid rgba(123,94,167,.18);border-radius:12px;padding:12px;margin-bottom:10px}
.pbhd{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:8px}
.pbt{font-size:.75rem;font-weight:700;margin-bottom:1px;color:var(--tx)}.pbd{font-size:.63rem;color:var(--mu)}
.pbn{font-size:1.7rem;font-weight:800;color:var(--pu);line-height:1}.pbn span{font-size:.68rem;font-weight:500;color:var(--mu)}
.pbr{height:6px;background:rgba(0,0,0,.07);border-radius:4px;overflow:hidden;margin-bottom:4px}
.pbf{height:100%;border-radius:4px}.pbl{font-size:.65rem;color:var(--mu)}
.scs{margin-bottom:10px}
.sct{font-size:.62rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--mu);margin-bottom:5px}
.arow{display:flex;align-items:center;gap:8px;padding:7px 9px;background:var(--bg);border-radius:7px;margin-bottom:4px;border:1px solid var(--bd)}
.anum{width:21px;height:21px;border-radius:5px;display:flex;align-items:center;justify-content:center;font-size:.63rem;font-weight:700;flex-shrink:0}
.anum.done{background:rgba(46,139,87,.15);color:var(--gn)}.anum.pend{background:rgba(212,137,26,.15);color:var(--or)}.anum.atras{background:rgba(192,57,43,.15);color:var(--rd)}
.ainf{flex:1;min-width:0}.adt{font-size:.76rem;font-weight:600;color:var(--tx)}.asb{font-size:.63rem;color:var(--mu)}
.achk{width:25px;height:25px;border-radius:6px;border:1.5px solid var(--bd);background:var(--sf);cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:.82rem;flex-shrink:0;color:var(--mu)}
.achk.done{background:rgba(46,139,87,.12);border-color:var(--gn);color:var(--gn)}
.adl{background:none;border:none;color:var(--dm);cursor:pointer;font-size:.82rem;padding:2px;flex-shrink:0}
.adl:hover{color:var(--rd)}
.baul{width:100%;background:var(--bg);color:var(--mu);border:1.5px dashed var(--bd);border-radius:9px;padding:8px;font-family:inherit;font-size:.75rem;font-weight:600;cursor:pointer;margin-bottom:6px}
.bsch{width:100%;background:rgba(123,94,167,.08);color:var(--pu);border:1.5px solid rgba(123,94,167,.25);border-radius:9px;padding:8px;font-family:inherit;font-size:.75rem;font-weight:600;cursor:pointer;margin-bottom:10px;display:flex;align-items:center;justify-content:center;gap:4px}
.bwa{width:100%;background:#25D366;color:#fff;border:none;border-radius:9px;padding:10px;font-family:inherit;font-size:.8rem;font-weight:700;cursor:pointer;display:flex;align-items:center;justify-content:center;gap:5px;margin-bottom:4px;box-shadow:0 2px 8px rgba(37,211,102,.3)}
.aviso{background:rgba(212,137,26,.08);border:1px solid rgba(212,137,26,.25);border-radius:8px;padding:10px;margin-bottom:12px;font-size:.74rem;color:var(--or);line-height:1.5}
.view{display:none}.view.active{display:block}
/* CONFIRMAÇÃO */
.conf-overlay{position:fixed;inset:0;z-index:300;background:rgba(0,0,0,.45);display:none;align-items:center;justify-content:center;padding:20px}
.conf-overlay.open{display:flex}
.conf-box{background:var(--sf);border-radius:14px;padding:22px;width:100%;max-width:320px;box-shadow:var(--sha2);text-align:center}
.conf-title{font-size:.95rem;font-weight:800;margin-bottom:8px;color:var(--tx)}
.conf-msg{font-size:.8rem;color:var(--mu);margin-bottom:18px;line-height:1.5}
.conf-btns{display:flex;gap:8px}
.conf-cancel{flex:1;background:var(--bg);color:var(--mu);border:1.5px solid var(--bd);border-radius:8px;padding:10px;font-family:inherit;font-size:.82rem;font-weight:600;cursor:pointer}
.conf-ok{flex:1;background:var(--rd);color:#fff;border:none;border-radius:8px;padding:10px;font-family:inherit;font-size:.82rem;font-weight:700;cursor:pointer}
/* FINANCEIRO */
.fin-summary{display:grid;grid-template-columns:1fr 1fr 1fr;gap:8px;padding:14px 16px 0}
.fin-card{background:var(--sf);border-radius:12px;padding:13px 10px;text-align:center;border:1px solid var(--bd);box-shadow:var(--sha)}
.fin-card-label{font-size:.58rem;font-weight:700;letter-spacing:.08em;text-transform:uppercase;color:var(--mu);margin-bottom:5px}
.fin-card-val{font-size:1.1rem;font-weight:800;line-height:1}
.fin-card-val.rec{color:var(--gn)}.fin-card-val.desp{color:var(--rd)}.fin-card-val.saldo.pos{color:var(--pu)}.fin-card-val.saldo.neg{color:var(--rd)}
.fin-mes{display:flex;align-items:center;justify-content:space-between;padding:12px 16px 0}
.fin-mes-label{font-size:.85rem;font-weight:700;color:var(--tx)}
.fin-mes-nav{display:flex;gap:5px}
.fin-mes-nav button{background:var(--sf);border:1px solid var(--bd);border-radius:6px;width:26px;height:26px;cursor:pointer;font-size:.85rem;display:flex;align-items:center;justify-content:center;color:var(--tx)}
.fin-item{display:flex;align-items:center;gap:10px;padding:10px 12px;background:var(--sf);border-radius:11px;margin-bottom:6px;border:1px solid var(--bd);box-shadow:var(--sha)}
.fin-icon{width:36px;height:36px;border-radius:9px;display:flex;align-items:center;justify-content:center;font-size:17px;flex-shrink:0}
.fin-info{flex:1;min-width:0}
.fin-desc{font-size:.85rem;font-weight:600;color:var(--tx)}
.fin-sub{font-size:.67rem;color:var(--mu);margin-top:1px}
.fin-val{font-size:.92rem;font-weight:800;flex-shrink:0}
.fin-val.rec{color:var(--gn)}.fin-val.desp{color:var(--rd)}
.fin-del{background:none;border:none;color:var(--dm);cursor:pointer;font-size:.82rem;padding:2px;flex-shrink:0}
</style>
</head>
<body>
<div class="app">
  <div class="hdr">
    <div class="htop">
      <div class="brand">
        <div class="bico"><svg viewBox="0 0 34 34" fill="none"><circle cx="13" cy="15" r="9" fill="#7B5EA7" opacity=".9"/><circle cx="21" cy="15" r="9" fill="#F0A030" opacity=".8"/><circle cx="17" cy="21" r="8" fill="#D94F7E" opacity=".85"/><circle cx="17" cy="10" r="4" fill="#4F8FD4" opacity=".7"/></svg></div>
        <div><div class="bnm">Método Praxis</div><div class="bsb">ciência e prática</div></div>
      </div>
      <div style="display:flex;align-items:center;gap:6px">
      <button onclick="exportarDados()" style="background:rgba(123,94,167,.12);border:1px solid rgba(123,94,167,.3);border-radius:8px;padding:3px 9px;font-family:inherit;font-size:.6rem;font-weight:700;color:var(--pu);cursor:pointer">💾 Salvar</button>
      <button onclick="document.getElementById('importInput').click()" style="background:rgba(46,139,87,.1);border:1px solid rgba(46,139,87,.25);border-radius:8px;padding:3px 9px;font-family:inherit;font-size:.6rem;font-weight:700;color:var(--gn);cursor:pointer">📂 Restaurar</button>
      <input type="file" id="importInput" accept=".json" style="display:none" onchange="importarDados(this)">
      <div class="obadge"><div class="odot"></div>Online</div>
    </div>
    </div>
    <div class="swp"><span class="sic">🔍</span><input class="sinp" id="sInp" type="text" placeholder="Buscar..." autocomplete="off"><button class="scl" id="sCl" onclick="clrS()">✕</button></div>
  </div>

  <div class="view active" id="vClientes">
    <div class="ph"><div><div class="pt">Clientes</div><div class="ps">Tutores</div></div><button class="bno" onclick="abrirNC()">+ Novo</button></div>
    <div class="flt"><button class="chip active">Todos</button></div>
    <div class="rc" id="cRc"></div><div class="lst" id="cLst"></div>
    <div class="empty" id="cEm"><div class="empty-i">👤</div><div class="empty-t">Nenhum cliente</div><div class="empty-s">Cadastre o primeiro cliente.</div></div>
  </div>

  <div class="view" id="vAlunos">
    <div class="ph"><div><div class="pt">Alunos</div><div class="ps">Adestramento</div></div><button class="bno" onclick="abrirNA()">+ Novo</button></div>
    <div class="flt"><button class="chip active" onclick="setF('todos',this,'a')">Todos</button><button class="chip" onclick="setF('ativo',this,'a')">Em andamento</button><button class="chip" onclick="setF('avaliacao',this,'a')">Avaliação</button><button class="chip" onclick="setF('concluido',this,'a')">Concluídos</button></div>
    <div class="rc" id="aRc"></div><div class="lst" id="aLst"></div>
    <div class="empty" id="aEm"><div class="empty-i">🐕</div><div class="empty-t">Nenhum aluno</div><div class="empty-s">Cadastre o primeiro aluno.</div></div>
  </div>

  <div class="view" id="vAgenda">
    <div class="ph" style="padding-bottom:0"><div><div class="pt">Agenda</div><div class="ps">Calendário</div></div></div>
    <div class="fin-mes" style="padding-top:12px"><div class="fin-mes-label" id="calM"></div><div class="fin-mes-nav"><button onclick="mvCal(-1)">‹</button><button onclick="mvCal(1)">›</button></div></div>
    <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:1px;padding:8px 14px 6px" id="calG"></div>
    <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:1px;padding:0 14px 4px" id="calDW"></div>
    <div style="padding:0 16px 105px" id="calDL"></div>
  </div>

  <div class="view" id="vCreche">
    <div class="ph"><div><div class="pt">Creche</div><div class="ps">Pacotes</div></div><button class="bno" onclick="abrirNCr()">+ Novo</button></div>
    <div class="flt"><button class="chip active" onclick="setF('todos',this,'cr')">Todos</button><button class="chip" onclick="setF('ativa',this,'cr')">Ativos</button><button class="chip" onclick="setF('concluida',this,'cr')">Encerrados</button></div>
    <div class="rc" id="crRc"></div><div class="lst" id="crLst"></div>
    <div class="empty" id="crEm"><div class="empty-i">🏠</div><div class="empty-t">Nenhuma creche</div><div class="empty-s">Cadastre o primeiro pacote.</div></div>
  </div>

  <div class="view" id="vHosp">
    <div class="ph"><div><div class="pt">Hospedagem</div><div class="ps">Reservas</div></div><button class="bno" onclick="abrirNH()">+ Nova</button></div>
    <div class="flt"><button class="chip active" onclick="setF('todos',this,'h')">Todos</button><button class="chip" onclick="setF('agendado',this,'h')">Agendados</button><button class="chip" onclick="setF('hospedado',this,'h')">Hospedados</button><button class="chip" onclick="setF('concluido',this,'h')">Concluídos</button></div>
    <div class="rc" id="hRc"></div><div class="lst" id="hLst"></div>
    <div class="empty" id="hEm"><div class="empty-i">🛏️</div><div class="empty-t">Nenhuma hospedagem</div><div class="empty-s">Cadastre a primeira reserva.</div></div>
  </div>

  <div class="view" id="vFinanceiro">
    <div class="ph"><div><div class="pt">Financeiro</div><div class="ps">Receitas e despesas</div></div><button class="bno" onclick="abrirNDesp()">+ Despesa</button></div>
    <div class="fin-summary" id="finSummary"></div>
    <div class="fin-mes" style="margin-top:12px"><div class="fin-mes-label" id="finMesLabel"></div><div class="fin-mes-nav"><button onclick="mvFin(-1)">‹</button><button onclick="mvFin(1)">›</button></div></div>
    <div class="flt"><button class="chip active" onclick="setFF('todos',this)">Todos</button><button class="chip" onclick="setFF('receita',this)">Receitas</button><button class="chip" onclick="setFF('despesa',this)">Despesas</button></div>
    <div class="lst" id="finLst"></div>
    <div class="empty" id="finEm"><div class="empty-i">💰</div><div class="empty-t">Nenhum lançamento</div><div class="empty-s">Cadastre pacotes ou adicione despesas.</div></div>
  </div>

  <div class="bnav">
    <button class="ni active" onclick="swV('Clientes',this)"><span class="ni-i">👥</span>Clientes</button>
    <button class="ni" onclick="swV('Alunos',this)"><span class="ni-i">🐶</span>Alunos</button>
    <button class="ni" onclick="swV('Agenda',this)"><span class="ni-i">📅</span>Agenda</button>
    <button class="ni" onclick="swV('Creche',this)"><span class="ni-i">🏠</span>Creche</button>
    <button class="ni" onclick="swV('Hosp',this)"><span class="ni-i">🛏️</span>Hosped.</button>
    <button class="ni" onclick="swV('Financeiro',this)"><span class="ni-i">💰</span>Financ.</button>
  </div>
</div>

<!-- CONFIRMAÇÃO -->
<div class="conf-overlay" id="mConf">
  <div class="conf-box">
    <div class="conf-title" id="confTitle">Confirmar</div>
    <div class="conf-msg" id="confMsg">Tem certeza?</div>
    <div class="conf-btns">
      <button class="conf-cancel" onclick="closeM('mConf')">Cancelar</button>
      <button class="conf-ok" onclick="execConf()">Excluir</button>
    </div>
  </div>
</div>

<!-- MODAL CLIENTE -->
<div class="mo" id="mCl"><div class="mdl">
  <div class="mhd"><div class="mtt" id="mClT">Novo Cliente</div><button class="mcl" onclick="closeM('mCl')">✕</button></div>
  <div class="mb">
    <div class="fr"><div class="fg"><label class="fl">Nome</label><input class="fi" id="cN" placeholder="Ana"></div><div class="fg"><label class="fl">Sobrenome</label><input class="fi" id="cS" placeholder="Silva"></div></div>
    <div class="fg"><label class="fl">WhatsApp</label><input class="fi" id="cW" type="tel" placeholder="(11) 99999-9999"></div>
    <div class="fg"><label class="fl">E-mail</label><input class="fi" id="cE" type="email" placeholder="ana@email.com"></div>
    <div class="fg"><label class="fl">Rua e número</label><input class="fi" id="cRu" placeholder="Rua das Flores, 123"></div>
    <div class="fr"><div class="fg"><label class="fl">Bairro</label><input class="fi" id="cBa" placeholder="Moema"></div><div class="fg"><label class="fl">Cidade</label><input class="fi" id="cCi" placeholder="São Paulo"></div></div>
    <div class="fr"><div class="fg"><label class="fl">CEP</label><input class="fi" id="cCe" placeholder="00000-000"></div><div class="fg"><label class="fl">Estado</label><input class="fi" id="cEs" placeholder="SP"></div></div>
    <div class="fg"><label class="fl">Complemento</label><input class="fi" id="cCo" placeholder="Apto 42"></div>
    <button class="bsv" id="bSvC" onclick="svCliente()">Cadastrar cliente</button>
  </div>
</div></div>

<!-- MODAL ALUNO -->
<div class="mo" id="mAl"><div class="mdl">
  <div class="mhd"><div class="mtt" id="mAlT">Novo Aluno</div><button class="mcl" onclick="closeM('mAl')">✕</button></div>
  <div class="mb">
    <div class="fr"><div class="fg"><label class="fl">Nome do cão</label><input class="fi" id="aN" placeholder="Ex: Thor"></div><div class="fg"><label class="fl">Raça</label><input class="fi" id="aR" placeholder="Ex: Labrador"></div></div>
    <div class="fr"><div class="fg"><label class="fl">Idade</label><input class="fi" id="aI" placeholder="Ex: 2 anos"></div><div class="fg"><label class="fl">Status</label><select class="fs" id="aSt"><option value="avaliacao">Avaliação</option><option value="ativo">Em andamento</option><option value="concluido">Concluído</option></select></div></div>
    <div class="fg"><label class="fl">Cliente (tutor)</label><select class="fs" id="aCl"></select></div>
    <div class="fg"><label class="fl">Observações</label><input class="fi" id="aOb" placeholder="Ex: Ansiedade de separação"></div>
    <div class="fg">
      <label class="fl">Serviços</label>
      <div style="display:flex;flex-wrap:wrap;gap:8px;margin-top:2px">
        <label style="display:flex;align-items:center;gap:6px;background:var(--bg);border:1.5px solid var(--bd);border-radius:8px;padding:7px 11px;cursor:pointer;font-size:.8rem;font-weight:600;color:var(--mu)">
          <input type="checkbox" id="svcAdest" value="adestramento" style="accent-color:var(--pu);width:15px;height:15px"> 🐕 Adestramento
        </label>
        <label style="display:flex;align-items:center;gap:6px;background:var(--bg);border:1.5px solid var(--bd);border-radius:8px;padding:7px 11px;cursor:pointer;font-size:.8rem;font-weight:600;color:var(--mu)">
          <input type="checkbox" id="svcCreche" value="creche" style="accent-color:var(--pu);width:15px;height:15px"> 🏠 Creche
        </label>
        <label style="display:flex;align-items:center;gap:6px;background:var(--bg);border:1.5px solid var(--bd);border-radius:8px;padding:7px 11px;cursor:pointer;font-size:.8rem;font-weight:600;color:var(--mu)">
          <input type="checkbox" id="svcHosp" value="hospedagem" style="accent-color:var(--pu);width:15px;height:15px"> 🛏️ Hospedagem
        </label>
      </div>
    </div>
    <div class="ssep">
      <div class="slbl">📦 Pacote de Aulas</div>
      <div class="fr"><div class="fg"><label class="fl">Total de aulas</label><input class="fi" id="aPk" type="number" min="1" placeholder="Ex: 8"></div><div class="fg"><label class="fl">Data de início</label><input class="fi" id="aSt2" type="date"></div></div>
      <div class="fg"><label class="fl">Dias e horários</label><div id="aDHSlots"></div><button type="button" class="btn-add-slot" onclick="addSlot('aDHSlots',null,null)">+ Adicionar dia</button></div>
      <div class="fg"><label class="fl">Valor do pacote (R$)</label><input class="fi" id="aVal" type="number" min="0" step="0.01" placeholder="Ex: 500,00"></div>
      <div class="fg"><label class="fl">Forma de pagamento</label>
        <div class="pgto-grid" id="aPgtoGrid">
          <button type="button" class="pgto-chip" data-p="PIX" onclick="selPgto('aPgtoGrid','aPgto',this)">PIX</button>
          <button type="button" class="pgto-chip" data-p="Dinheiro" onclick="selPgto('aPgtoGrid','aPgto',this)">Dinheiro</button>
          <button type="button" class="pgto-chip" data-p="Cartão crédito" onclick="selPgto('aPgtoGrid','aPgto',this)">Crédito</button>
          <button type="button" class="pgto-chip" data-p="Cartão débito" onclick="selPgto('aPgtoGrid','aPgto',this)">Débito</button>
          <button type="button" class="pgto-chip" data-p="Transferência" onclick="selPgto('aPgtoGrid','aPgto',this)">Transfer.</button>
        </div>
        <input class="fi" id="aPgto" placeholder="Ou descreva a forma" style="margin-top:6px">
      </div>
      <div class="fg"><label class="fl">Status do pagamento</label>
        <select class="fs" id="aStPgto"><option value="pendente">Pendente</option><option value="pago">Pago</option><option value="parcial">Parcialmente pago</option></select>
      </div>
    </div>
    <button class="bsv" id="bSvA" onclick="svAluno()">Cadastrar aluno</button>
  </div>
</div></div>

<!-- DET CLIENTE -->
<div class="mo" id="mDC"><div class="mdl"><div class="mhd"><div class="mtt">Perfil do Cliente</div><button class="mcl" onclick="closeM('mDC')">✕</button></div><div class="mb" id="dCB"></div></div></div>
<!-- DET ALUNO -->
<div class="mo" id="mDA"><div class="mdl"><div class="mhd"><div class="mtt">Perfil do Aluno</div><button class="mcl" onclick="closeM('mDA')">✕</button></div><div class="mb" id="dAB"></div></div></div>
<!-- EDIT SCH -->
<div class="mo" id="mES"><div class="mdl">
  <div class="mhd"><div class="mtt">✏️ Editar Agendamento</div><button class="mcl" onclick="closeM('mES')">✕</button></div>
  <div class="mb">
    <div class="aviso">⚠️ Aulas realizadas ficam. Só as pendentes serão reagendadas.</div>
    <div class="fg"><label class="fl">Novos dias e horários</label><div id="eDHSlots"></div><button type="button" class="btn-add-slot" onclick="addSlot('eDHSlots',null,null)">+ Adicionar dia</button></div>
    <div class="fg"><label class="fl">A partir de</label><input class="fi" id="eIni" type="date"></div>
    <button class="bsv" onclick="svEditSch()">Salvar agendamento</button>
  </div>
</div></div>
<!-- AULA AVULSA -->
<div class="mo" id="mAv"><div class="mdl">
  <div class="mhd"><div class="mtt">Adicionar Aula</div><button class="mcl" onclick="closeM('mAv')">✕</button></div>
  <div class="mb">
    <div class="fr"><div class="fg"><label class="fl">Data</label><input class="fi" id="avD" type="date"></div><div class="fg"><label class="fl">Horário</label><input class="fi" id="avH" type="time"></div></div>
    <div class="fg"><label class="fl">Observações</label><input class="fi" id="avO" placeholder="Opcional"></div>
    <button class="bsv" onclick="svAvulsa()">Adicionar</button>
  </div>
</div></div>

<!-- MODAL CRECHE -->
<div class="mo" id="mCr"><div class="mdl">
  <div class="mhd"><div class="mtt" id="mCrT">Nova Creche</div><button class="mcl" onclick="closeM('mCr')">✕</button></div>
  <div class="mb">
    <div class="fg"><label class="fl">Cliente (tutor)</label><select class="fs" id="crCl" onchange="crCliChange(this.value)"></select></div>
    <div class="fg"><label class="fl">Cão</label><select class="fs" id="crAl" onchange="crCliChange(document.getElementById('crCl').value)"></select></div>
    <div class="fg"><label class="fl">Tipo</label><div class="tgr"><button type="button" class="tpb sel" id="tpM" onclick="selTipo('mensal')">📅 Mensal</button><button type="button" class="tpb" id="tpT" onclick="selTipo('trimestral')">📆 Trimestral</button></div></div>
    <div class="fg"><label class="fl">Dias da semana</label>
      <div style="display:grid;grid-template-columns:repeat(7,1fr);gap:3px" id="crDG">
        <button type="button" class="tpb" data-d="0" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Dom</button>
        <button type="button" class="tpb" data-d="1" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Seg</button>
        <button type="button" class="tpb" data-d="2" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Ter</button>
        <button type="button" class="tpb" data-d="3" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Qua</button>
        <button type="button" class="tpb" data-d="4" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Qui</button>
        <button type="button" class="tpb" data-d="5" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Sex</button>
        <button type="button" class="tpb" data-d="6" onclick="this.classList.toggle('sel')" style="padding:7px 0;font-size:.62rem">Sáb</button>
      </div>
    </div>
    <div class="fr"><div class="fg"><label class="fl">Entrada</label><input class="fi" id="crEn" type="time" value="07:00"></div><div class="fg"><label class="fl">Saída</label><input class="fi" id="crSa" type="time" value="18:00"></div></div>
    <div class="fg"><label class="fl">Início</label><input class="fi" id="crIn" type="date"></div>
    <div class="trow"><div><div class="tlb">Transporte incluso</div><div class="tsb">Busca e/ou retorno</div></div><label class="tg"><input type="checkbox" id="crTr"><span class="tsl"></span></label></div>
    <div class="ssep">
      <div class="slbl">💰 Pagamento</div>
      <div class="fg"><label class="fl">Valor (R$)</label><input class="fi" id="crVal" type="number" min="0" step="0.01" placeholder="Ex: 800,00"></div>
      <div class="fg"><label class="fl">Forma de pagamento</label>
        <div class="pgto-grid" id="crPgtoGrid">
          <button type="button" class="pgto-chip" data-p="PIX" onclick="selPgto('crPgtoGrid','crPgto',this)">PIX</button>
          <button type="button" class="pgto-chip" data-p="Dinheiro" onclick="selPgto('crPgtoGrid','crPgto',this)">Dinheiro</button>
          <button type="button" class="pgto-chip" data-p="Cartão crédito" onclick="selPgto('crPgtoGrid','crPgto',this)">Crédito</button>
          <button type="button" class="pgto-chip" data-p="Cartão débito" onclick="selPgto('crPgtoGrid','crPgto',this)">Débito</button>
          <button type="button" class="pgto-chip" data-p="Transferência" onclick="selPgto('crPgtoGrid','crPgto',this)">Transfer.</button>
        </div>
        <input class="fi" id="crPgto" placeholder="Ou descreva a forma" style="margin-top:6px">
      </div>
      <div class="fg"><label class="fl">Status do pagamento</label><select class="fs" id="crStPgto"><option value="pendente">Pendente</option><option value="pago">Pago</option><option value="parcial">Parcialmente pago</option></select></div>
    </div>
    <div class="fg"><label class="fl">Observações</label><input class="fi" id="crOb" placeholder="Ex: Alérgico a ração X"></div>
    <button class="bsv" id="bSvCr" onclick="svCreche()">Cadastrar pacote</button>
  </div>
</div></div>
<!-- DET CRECHE -->
<div class="mo" id="mDCr"><div class="mdl"><div class="mhd"><div class="mtt">Pacote de Creche</div><button class="mcl" onclick="closeM('mDCr')">✕</button></div><div class="mb" id="dCrB"></div></div></div>

<!-- MODAL HOSPEDAGEM -->
<div class="mo" id="mHo"><div class="mdl">
  <div class="mhd"><div class="mtt" id="mHoT">Nova Hospedagem</div><button class="mcl" onclick="closeM('mHo')">✕</button></div>
  <div class="mb">
    <div class="fg"><label class="fl">Cliente (tutor)</label><select class="fs" id="hoCl" onchange="hoCliChange(this.value)"></select></div>
    <div class="fg"><label class="fl">Cão</label><select class="fs" id="hoAl" onchange="hoCliChange(document.getElementById('hoCl').value)"></select></div>
    <div class="fr"><div class="fg"><label class="fl">Check-in</label><input class="fi" id="hoCI" type="date"></div><div class="fg"><label class="fl">Check-out</label><input class="fi" id="hoCO" type="date"></div></div>
    <div class="fg"><label class="fl">Status</label><select class="fs" id="hoSt"><option value="agendado">Agendado</option><option value="hospedado">Hospedado</option><option value="concluido">Concluído</option></select></div>
    <div class="ssep">
      <div class="slbl">💰 Pagamento</div>
      <div class="fg"><label class="fl">Valor (R$)</label><input class="fi" id="hoVal" type="number" min="0" step="0.01" placeholder="Ex: 350,00"></div>
      <div class="fg"><label class="fl">Forma de pagamento</label>
        <div class="pgto-grid" id="hoPgtoGrid">
          <button type="button" class="pgto-chip" data-p="PIX" onclick="selPgto('hoPgtoGrid','hoPgto',this)">PIX</button>
          <button type="button" class="pgto-chip" data-p="Dinheiro" onclick="selPgto('hoPgtoGrid','hoPgto',this)">Dinheiro</button>
          <button type="button" class="pgto-chip" data-p="Cartão crédito" onclick="selPgto('hoPgtoGrid','hoPgto',this)">Crédito</button>
          <button type="button" class="pgto-chip" data-p="Cartão débito" onclick="selPgto('hoPgtoGrid','hoPgto',this)">Débito</button>
          <button type="button" class="pgto-chip" data-p="Transferência" onclick="selPgto('hoPgtoGrid','hoPgto',this)">Transfer.</button>
        </div>
        <input class="fi" id="hoPgto" placeholder="Ou descreva a forma" style="margin-top:6px">
      </div>
      <div class="fg"><label class="fl">Status do pagamento</label><select class="fs" id="hoStPgto"><option value="pendente">Pendente</option><option value="pago">Pago</option><option value="parcial">Parcialmente pago</option></select></div>
    </div>
    <div class="fg"><label class="fl">Observações</label><input class="fi" id="hoOb" placeholder="Ex: Traz ração própria"></div>
    <button class="bsv" id="bSvH" onclick="svHosp()">Cadastrar hospedagem</button>
  </div>
</div></div>
<!-- DET HOSP -->
<div class="mo" id="mDH"><div class="mdl"><div class="mhd"><div class="mtt">Hospedagem</div><button class="mcl" onclick="closeM('mDH')">✕</button></div><div class="mb" id="dHB"></div></div></div>

<!-- MODAL DESPESA -->
<div class="mo" id="mDesp"><div class="mdl">
  <div class="mhd"><div class="mtt" id="mDespT">Nova Despesa</div><button class="mcl" onclick="closeM('mDesp')">✕</button></div>
  <div class="mb">
    <div class="fg"><label class="fl">Descrição</label><input class="fi" id="dDesc" placeholder="Ex: Ração, veterinário, equipamento..."></div>
    <div class="fr"><div class="fg"><label class="fl">Valor (R$)</label><input class="fi" id="dVal" type="number" min="0" step="0.01" placeholder="0,00"></div><div class="fg"><label class="fl">Data</label><input class="fi" id="dData" type="date"></div></div>
    <div class="fg"><label class="fl">Categoria</label>
      <select class="fs" id="dCat">
        <option value="Alimentação">🍖 Alimentação</option>
        <option value="Veterinário">🏥 Veterinário</option>
        <option value="Equipamentos">🎾 Equipamentos</option>
        <option value="Transporte">🚗 Transporte</option>
        <option value="Marketing">📢 Marketing</option>
        <option value="Aluguel">🏢 Aluguel</option>
        <option value="Outros">📦 Outros</option>
      </select>
    </div>
    <div class="fg"><label class="fl">Forma de pagamento</label>
      <div class="pgto-grid" id="dPgtoGrid">
        <button type="button" class="pgto-chip" data-p="PIX" onclick="selPgto('dPgtoGrid','dPgto',this)">PIX</button>
        <button type="button" class="pgto-chip" data-p="Dinheiro" onclick="selPgto('dPgtoGrid','dPgto',this)">Dinheiro</button>
        <button type="button" class="pgto-chip" data-p="Cartão crédito" onclick="selPgto('dPgtoGrid','dPgto',this)">Crédito</button>
        <button type="button" class="pgto-chip" data-p="Cartão débito" onclick="selPgto('dPgtoGrid','dPgto',this)">Débito</button>
      </div>
      <input class="fi" id="dPgto" placeholder="Ou descreva a forma" style="margin-top:6px">
    </div>
    <div class="fg"><label class="fl">Observações</label><input class="fi" id="dObs" placeholder="Opcional"></div>
    <button class="bsv" onclick="svDesp()">Registrar despesa</button>
  </div>
</div></div>

<script>
var DPT=['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'];
var DFL=['Domingo','Segunda-feira','Terça-feira','Quarta-feira','Quinta-feira','Sexta-feira','Sábado'];
var MES=['Janeiro','Fevereiro','Março','Abril','Maio','Junho','Julho','Agosto','Setembro','Outubro','Novembro','Dezembro'];
var AVBG=['#E8D5F5','#D5E8F5','#F5E8D5','#D5F5E8','#F5D5E8','#E8F5D5'];
var CLBG=['#EDE0F7','#D0E8F7','#F7ECD0','#D0F7E8','#F7D0E0','#E8F7D0'];
var hoje=new Date().toISOString().slice(0,10);

var clientes=[
  {id:1,nome:'Aline',sobrenome:'Martins',whatsapp:'11991110001',email:'aline@email.com',rua:'Rua das Flores, 123',bairro:'Jardins',cidade:'São Paulo',estado:'SP',cep:'01234-000',comp:''},
  {id:2,nome:'Ingrid',sobrenome:'Leite',whatsapp:'11991110002',email:'ingrid@email.com',rua:'Av. Paulista, 456',bairro:'Bela Vista',cidade:'São Paulo',estado:'SP',cep:'01310-000',comp:'Apto 12'},
  {id:3,nome:'Antônio',sobrenome:'Maximo',whatsapp:'11991110003',email:'antonio@email.com',rua:'Rua XV, 78',bairro:'Centro',cidade:'Campinas',estado:'SP',cep:'13010-000',comp:''},
  {id:4,nome:'Thais',sobrenome:'Costa',whatsapp:'11991110004',email:'thais@email.com',rua:'Rua Augusta, 900',bairro:'Consolação',cidade:'São Paulo',estado:'SP',cep:'01304-000',comp:''},
  {id:5,nome:'Leomara',sobrenome:'Paganelli',whatsapp:'11991110005',email:'leomara@email.com',rua:'Rua Pamplona, 55',bairro:'Jardim Paulista',cidade:'São Paulo',estado:'SP',cep:'01405-000',comp:''},
  {id:6,nome:'Santiago',sobrenome:'Pascual',whatsapp:'11991110007',email:'santiago@email.com',rua:'Rua Oscar Freire, 200',bairro:'Cerqueira César',cidade:'São Paulo',estado:'SP',cep:'01426-000',comp:''}
];

function gAulas(ini,dh,tot){
  var l=[];
  if(!ini||!dh||!dh.length||!tot) return l;
  var base=new Date(ini+'T12:00:00').getTime();
  var maxDias=tot*21+90;
  for(var day=0;day<maxDias&&l.length<tot;day++){
    var dd=new Date(base+day*86400000);
    var dow=dd.getDay();
    var ds=dd.getFullYear()+'-'+('0'+(dd.getMonth()+1)).slice(-2)+'-'+('0'+dd.getDate()).slice(-2);
    for(var k=0;k<dh.length;k++){
      if(dh[k].dia===dow&&l.length<tot){
        l.push({data:ds,hora:dh[k].hora,feita:false,obs:''});
      }
    }
  }
  return l;
}

var alunos=[
  {id:1,nome:'Farofa',raca:'Srd',idade:'4 anos',status:'ativo',clienteId:1,obs:'Reatividade',pacote:{total:10,diasHoras:[{dia:2,hora:'09:00'},{dia:4,hora:'11:00'}],inicio:'2026-04-10',valor:600,formaPgto:'PIX',statusPgto:'pago'},aulas:gAulas('2026-04-10',[{dia:2,hora:'09:00'},{dia:4,hora:'11:00'}],10)},
  {id:2,nome:'Apollo',raca:'Srd',idade:'2 anos',status:'ativo',clienteId:2,obs:'Puxão na guia',pacote:{total:8,diasHoras:[{dia:3,hora:'10:00'},{dia:5,hora:'10:00'}],inicio:'2026-04-15',valor:500,formaPgto:'Dinheiro',statusPgto:'pendente'},aulas:gAulas('2026-04-15',[{dia:3,hora:'10:00'},{dia:5,hora:'10:00'}],8)},
  {id:3,nome:'Max',raca:'Srd',idade:'8 meses',status:'avaliacao',clienteId:3,obs:'Filhote',pacote:null,aulas:[]},
  {id:4,nome:'Ziggy',raca:'Border Collie',idade:'3 anos',status:'ativo',clienteId:4,obs:'Ansiedade',pacote:{total:12,diasHoras:[{dia:1,hora:'08:00'},{dia:3,hora:'08:00'},{dia:6,hora:'09:00'}],inicio:'2026-03-17',valor:720,formaPgto:'PIX',statusPgto:'pago'},aulas:gAulas('2026-03-17',[{dia:1,hora:'08:00'},{dia:3,hora:'08:00'},{dia:6,hora:'09:00'}],12)},
  {id:5,nome:'Sophie',raca:'Yorkshire',idade:'3 anos',status:'concluido',clienteId:5,obs:'Latidos',pacote:{total:8,diasHoras:[{dia:2,hora:'17:00'},{dia:5,hora:'17:00'}],inicio:'2026-01-07',valor:500,formaPgto:'Cartão crédito',statusPgto:'pago'},aulas:gAulas('2026-01-07',[{dia:2,hora:'17:00'},{dia:5,hora:'17:00'}],8)},
  {id:6,nome:'Cabron',raca:'Chihuahua',idade:'1 ano',status:'ativo',clienteId:6,obs:'Agressividade',pacote:{total:10,diasHoras:[{dia:1,hora:'16:00'},{dia:4,hora:'16:00'}],inicio:'2026-04-07',valor:600,formaPgto:'PIX',statusPgto:'parcial'},aulas:gAulas('2026-04-07',[{dia:1,hora:'16:00'},{dia:4,hora:'16:00'}],10)}
];
// Garantir campo servicos em alunos existentes
for(var i=0;i<alunos.length;i++){
  if(!alunos[i].servicos) alunos[i].servicos=['adestramento'];
}
for(var i=0;i<alunos.length;i++) for(var j=0;j<alunos[i].aulas.length;j++) if(alunos[i].aulas[j].data<hoje&&Math.random()>0.4) alunos[i].aulas[j].feita=true;
for(var j=0;j<alunos[4].aulas.length;j++) alunos[4].aulas[j].feita=true;

var creches=[
  {id:1,clienteId:1,alunoId:1,tipo:'mensal',dias:[1,2,3,4,5],entrada:'07:30',saida:'18:00',inicio:'2026-05-01',transporte:true,status:'ativa',obs:'',valor:900,formaPgto:'PIX',statusPgto:'pago'},
  {id:2,clienteId:3,alunoId:3,tipo:'trimestral',dias:[1,3,5],entrada:'08:00',saida:'17:00',inicio:'2026-04-01',transporte:false,status:'ativa',obs:'',valor:2400,formaPgto:'Cartão crédito',statusPgto:'pendente'}
];
var hospedagens=[
  {id:1,clienteId:2,alunoId:2,checkin:'2026-05-15',checkout:'2026-05-20',status:'agendado',obs:'Traz cama própria',valor:350,formaPgto:'PIX',statusPgto:'pendente'},
  {id:2,clienteId:4,alunoId:4,checkin:'2026-04-20',checkout:'2026-04-25',status:'concluido',obs:'',valor:350,formaPgto:'Dinheiro',statusPgto:'pago'}
];
var despesas=[
  {id:1,descricao:'Ração para hóspedes',valor:180,data:'2026-05-02',categoria:'Alimentação',formaPgto:'PIX',obs:''},
  {id:2,descricao:'Coleira e guia',valor:95,data:'2026-04-15',categoria:'Equipamentos',formaPgto:'Dinheiro',obs:''}
];

var nxC=7,nxA=7,nxCr=3,nxH=3,nxD=3;
var edC=null,edA=null,edCr=null,edH=null,auAlvo=null;
var fA='todos',fCr='todos',fH='todos',fFin='todos',sq='';
var calY=new Date().getFullYear(),calM2=new Date().getMonth(),calDS=null;
var finY=new Date().getFullYear(),finM=new Date().getMonth();
var crTipo='mensal';

// CONFIRMAÇÃO
var confFn=null;
function pedirConf(titulo,msg,fn){confFn=fn;document.getElementById('confTitle').textContent=titulo;document.getElementById('confMsg').textContent=msg;document.getElementById('mConf').classList.add('open');}
function execConf(){document.getElementById('mConf').classList.remove('open');if(confFn){confFn();confFn=null;}}

// UTILS
function slbl(s){return{ativo:'Ativo',ativa:'Ativa',concluido:'Concluído',concluida:'Encerrado',avaliacao:'Avaliação',agendado:'Agendado',hospedado:'Hospedado',pago:'Pago',pendente:'Pendente',parcial:'Parcial'}[s]||s;}
function nCli(id){for(var i=0;i<clientes.length;i++) if(clientes[i].id===id) return clientes[i].nome+' '+clientes[i].sobrenome; return'—';}
function nCao(id){for(var i=0;i<alunos.length;i++) if(alunos[i].id===id) return alunos[i].nome; return'—';}
function fmtD(d){if(!d)return'—';var p=d.split('-');return p[2]+'/'+p[1]+'/'+p[0];}
function fmtDow(d){return DFL[new Date(d+'T12:00').getDay()];}
function fmtR(v){return'R$ '+parseFloat(v||0).toFixed(2).replace('.',',').replace(/\B(?=(\d{3})+(?!\d))/g,'.');}
function pCol(p){return p>=80?'var(--rd)':p>=50?'var(--or)':'var(--gn)';}
function dhStr(dh){return dh.map(function(x){return DPT[x.dia]+' '+x.hora;}).join(' · ');}
function auSt(au){if(au.feita)return'done';if(au.data<hoje)return'atras';return'pend';}
function endStr(c){var p=[];if(c.rua)p.push(c.rua);if(c.comp)p.push(c.comp);if(c.bairro)p.push(c.bairro);if(c.cidade)p.push(c.cidade+(c.estado?' - '+c.estado:''));if(c.cep)p.push('CEP '+c.cep);return p.join(', ')||'—';}
function fillCl(sid,sel){var s=document.getElementById(sid);s.innerHTML='<option value="">— Selecione o tutor —</option>';for(var i=0;i<clientes.length;i++) s.innerHTML+='<option value="'+clientes[i].id+'"'+(clientes[i].id===sel?' selected':'')+'>'+clientes[i].nome+' '+clientes[i].sobrenome+'</option>';s.innerHTML+='<option value="novo">+ Cadastrar novo cliente</option>';}
function crCliChange(val){
  if(val==='novo'||!parseInt(val)) fillCao('crAl',null,null,'creche');
  else fillCao('crAl',parseInt(val),null,'creche');
}
function hoCliChange(val){
  if(val==='novo'||!parseInt(val)) fillCao('hoAl',null,null,'hospedagem');
  else fillCao('hoAl',parseInt(val),null,'hospedagem');
}
function criarCliSeNovo(nomeId,sobId,waId){
  var nome=document.getElementById(nomeId).value.trim();
  if(!nome) return null;
  var obj={id:nxC++,nome:nome,sobrenome:document.getElementById(sobId).value.trim(),whatsapp:document.getElementById(waId).value.trim(),email:'',rua:'',bairro:'',cidade:'',estado:'',cep:'',comp:''};
  clientes.push(obj);
  return obj.id;
}
function fillCao(sid,cliId,sel,svc){var s=document.getElementById(sid);s.innerHTML='<option value="">— Selecione o cão —</option>';var l=alunos.filter(function(a){var matchCli=!cliId||a.clienteId===cliId;var matchSvc=!svc||!a.servicos||a.servicos.length===0||a.servicos.indexOf(svc)>=0;return matchCli&&matchSvc;});for(var i=0;i<l.length;i++) s.innerHTML+='<option value="'+l[i].id+'"'+(l[i].id===sel?' selected':'')+'>'+l[i].nome+' ('+l[i].raca+') — '+nCli(l[i].clienteId)+'</option>';}
function toggleNovoCao(divId,val){var d=document.getElementById(divId);if(d) d.style.display=(val==='novo'?'block':'none');}
function criarCaoSeNovo(cliId,nomeId,racaId,idadeId){
  var nome=document.getElementById(nomeId).value.trim();
  if(!nome) return null;
  var raca=document.getElementById(racaId).value.trim()||'Srd';
  var idade=document.getElementById(idadeId).value.trim()||'—';
  var novo={id:nxA++,nome:nome,raca:raca,idade:idade,status:'ativo',clienteId:cliId,obs:'',pacote:null,aulas:[]};
  alunos.push(novo);
  return novo.id;
}
function updCao(sid,cliId){fillCao(sid,cliId,null,null);}
function selTipo(t){crTipo=t;document.getElementById('tpM').classList.toggle('sel',t==='mensal');document.getElementById('tpT').classList.toggle('sel',t==='trimestral');}
function selPgto(gid,iid,btn){document.querySelectorAll('#'+gid+' .pgto-chip').forEach(function(b){b.classList.remove('sel');});btn.classList.add('sel');document.getElementById(iid).value=btn.dataset.p;}
function resetPgtoGrid(gid,iid){document.querySelectorAll('#'+gid+' .pgto-chip').forEach(function(b){b.classList.remove('sel');});document.getElementById(iid).value='';}
function preselPgto(gid,iid,val){resetPgtoGrid(gid,iid);if(!val) return;document.querySelectorAll('#'+gid+' .pgto-chip').forEach(function(b){if(b.dataset.p===val) b.classList.add('sel');});document.getElementById(iid).value=val;}

// SLOTS DIA+HORA
function addSlot(cid,dia,hora){var c=document.getElementById(cid);var div=document.createElement('div');div.className='dh-slot';var opts='';for(var i=0;i<DPT.length;i++) opts+='<option value="'+i+'"'+(dia===i?' selected':'')+'>'+DPT[i]+'</option>';div.innerHTML='<select class="dh-sel">'+opts+'</select><input type="time" class="dh-time" value="'+(hora||'09:00')+'"><button type="button" class="dh-rm" onclick="this.parentNode.remove()">✕</button>';c.appendChild(div);}
function getSlots(cid){var r=[];document.querySelectorAll('#'+cid+' .dh-slot').forEach(function(row){r.push({dia:parseInt(row.querySelector('.dh-sel').value),hora:row.querySelector('.dh-time').value||'09:00'});});return r;}
function clearSlots(cid){document.getElementById(cid).innerHTML='';}
function loadSlots(cid,dh){clearSlots(cid);if(dh) for(var i=0;i<dh.length;i++) addSlot(cid,dh[i].dia,dh[i].hora);}

// MODAIS
function openM(id){document.getElementById(id).classList.add('open');}
function closeM(id){document.getElementById(id).classList.remove('open');}
var mos=document.querySelectorAll('.mo');
for(var mi=0;mi<mos.length;mi++)(function(m){m.addEventListener('click',function(e){if(e.target===m) closeM(m.id);});})(mos[mi]);

// VIEWS
function swV(v,btn){
  document.querySelectorAll('.view').forEach(function(e){e.classList.remove('active');});
  document.querySelectorAll('.ni').forEach(function(e){e.classList.remove('active');});
  document.getElementById('v'+v).classList.add('active');btn.classList.add('active');
  sq='';document.getElementById('sInp').value='';document.getElementById('sCl').classList.remove('v');
  document.getElementById('sInp').style.display=(v==='Clientes'||v==='Alunos')?'':'none';
  
// ── BACKUP / RESTAURAR ────────────────────────────────────────────────
function exportarDados(){
  var dados={clientes:clientes,alunos:alunos,creches:creches,hospedagens:hospedagens,despesas:despesas,nxC:nxC,nxA:nxA,nxCr:nxCr,nxH:nxH,nxD:nxD};
  var json=JSON.stringify(dados);
  var blob=new Blob([json],{type:'application/json'});
  var url=URL.createObjectURL(blob);
  var a=document.createElement('a');
  var d=new Date();
  a.href=url;
  a.download='praxis-backup-'+d.getFullYear()+('0'+(d.getMonth()+1)).slice(-2)+('0'+d.getDate()).slice(-2)+'.json';
  a.click();
  URL.revokeObjectURL(url);
}
function importarDados(input){
  var file=input.files[0];if(!file) return;
  var reader=new FileReader();
  reader.onload=function(e){
    try{
      var dados=JSON.parse(e.target.result);
      if(dados.clientes) clientes=dados.clientes;
      if(dados.alunos) alunos=dados.alunos;
      if(dados.creches) creches=dados.creches;
      if(dados.hospedagens) hospedagens=dados.hospedagens;
      if(dados.despesas) despesas=dados.despesas;
      if(dados.nxC) nxC=dados.nxC;
      if(dados.nxA) nxA=dados.nxA;
      if(dados.nxCr) nxCr=dados.nxCr;
      if(dados.nxH) nxH=dados.nxH;
      if(dados.nxD) nxD=dados.nxD;
      renderAll();
      alert('Dados restaurados com sucesso!');
    }catch(err){alert('Arquivo inválido.');}
  };
  reader.readAsText(file);
  input.value='';
}

renderAll();
}
document.getElementById('sInp').addEventListener('input',function(){sq=this.value;document.getElementById('sCl').classList.toggle('v',this.value.length>0);renderAll();});
function clrS(){document.getElementById('sInp').value='';sq='';document.getElementById('sCl').classList.remove('v');renderAll();}
function setF(f,btn,v){if(v==='a')fA=f;else if(v==='cr')fCr=f;else if(v==='h')fH=f;btn.closest('.flt').querySelectorAll('.chip').forEach(function(c){c.classList.remove('active');});btn.classList.add('active');renderAll();}
function setFF(f,btn){fFin=f;btn.closest('.flt').querySelectorAll('.chip').forEach(function(c){c.classList.remove('active');});btn.classList.add('active');rFinanceiro();}

// RENDER CLIENTES
function rClientes(){
  var q=sq.toLowerCase();
  var list=clientes.filter(function(c){return !q||(c.nome+' '+c.sobrenome).toLowerCase().indexOf(q)>=0||(c.cidade||'').toLowerCase().indexOf(q)>=0;});
  var el=document.getElementById('cLst'),em=document.getElementById('cEm');
  document.getElementById('cRc').innerHTML='<strong>'+list.length+'</strong> cliente'+(list.length!==1?'s':'')+' encontrado'+(list.length!==1?'s':'');
  if(!list.length){el.innerHTML='';em.classList.add('v');return;}em.classList.remove('v');
  var h='';for(var i=0;i<list.length;i++){var c=list[i],sa=alunos.filter(function(a){return a.clienteId===c.id;}).length;h+='<div class="card" style="animation-delay:'+(i*28)+'ms" onclick="opDC('+c.id+')">'+'<div class="av" style="background:'+CLBG[i%CLBG.length]+'">👤</div>'+'<div class="ci"><div class="cnr"><span class="cn">'+c.nome+' '+c.sobrenome+'</span></div><div class="cm">'+(c.bairro&&c.cidade?c.bairro+', '+c.cidade:c.cidade||'—')+' · <strong>'+sa+' cão(ões)</strong></div></div><div class="cr"><span class="chv">›</span></div></div>';}
  el.innerHTML=h;
}

// RENDER ALUNOS
function rAlunos(){
  var q=sq.toLowerCase();
  var list=alunos.filter(function(a){return(fA==='todos'||a.status===fA)&&(!q||a.nome.toLowerCase().indexOf(q)>=0||a.raca.toLowerCase().indexOf(q)>=0||nCli(a.clienteId).toLowerCase().indexOf(q)>=0);});
  var el=document.getElementById('aLst'),em=document.getElementById('aEm');
  document.getElementById('aRc').innerHTML='<strong>'+list.length+'</strong> aluno'+(list.length!==1?'s':'')+' encontrado'+(list.length!==1?'s':'');
  if(!list.length){el.innerHTML='';em.classList.add('v');return;}em.classList.remove('v');
  var h='';
  for(var i=0;i<list.length;i++){
    var a=list[i];
    var ft=a.aulas.filter(function(x){return x.feita;}).length;
    var tot=a.pacote?a.pacote.total:0,pct=tot>0?Math.round(ft/tot*100):0;
    var at=a.aulas.filter(function(x){return !x.feita&&x.data<hoje;}).length;
    var bar=tot>0?('<div class="pm"><div class="pmb"><div class="pmf" style="width:'+pct+'%;background:'+pCol(pct)+'"></div></div><div class="pml">'+ft+'/'+tot+' aulas'+(at>0?' · <span style="color:var(--rd)">'+at+' atr.</span>':'')+'</div></div>'):'';
    var valTag=a.pacote&&a.pacote.valor?('<span class="badge '+a.pacote.statusPgto+'" style="margin-top:2px">'+fmtR(a.pacote.valor)+'</span>'):'';
    h+='<div class="card" style="animation-delay:'+(i*28)+'ms" onclick="opDA('+a.id+')">'
      +'<div class="av" style="background:'+AVBG[(a.id-1)%AVBG.length]+'">🐕</div>'
      +'<div class="ci"><div class="cnr"><span class="cn">'+a.nome+'</span><span class="cs">'+a.idade+'</span></div>'
      +'<div class="cm">'+a.raca+' · <strong>'+nCli(a.clienteId)+'</strong></div>'+bar+'</div>'
      +'<div class="cr"><span class="badge '+a.status+'">'+slbl(a.status)+'</span>'+valTag+'</div></div>';
  }
  el.innerHTML=h;
}

// RENDER CRECHE
function rCreche(){
  var list=creches.filter(function(c){return fCr==='todos'||c.status===fCr;});
  var el=document.getElementById('crLst'),em=document.getElementById('crEm');
  document.getElementById('crRc').innerHTML='<strong>'+list.length+'</strong> pacote'+(list.length!==1?'s':'')+' encontrado'+(list.length!==1?'s':'');
  if(!list.length){el.innerHTML='';em.classList.add('v');return;}em.classList.remove('v');
  var h='';for(var i=0;i<list.length;i++){var cr=list[i];h+='<div class="card" style="animation-delay:'+(i*28)+'ms" onclick="opDCr('+cr.id+')">'+'<div class="av" style="background:#D5EAF7">🏠</div>'+'<div class="ci"><div class="cnr"><span class="cn">'+nCao(cr.alunoId)+'</span><span class="cs">'+(cr.tipo==='mensal'?'Mensal':'Trimestral')+'</span></div><div class="cm">'+nCli(cr.clienteId)+'</div><div class="cm">'+(cr.transporte?'🚗 Com transporte':'Sem transporte')+'</div></div>'+'<div class="cr"><span class="badge '+cr.status+'">'+slbl(cr.status)+'</span>'+(cr.valor?'<span class="badge '+cr.statusPgto+'">'+fmtR(cr.valor)+'</span>':'')+'</div></div>';}
  el.innerHTML=h;
}

// RENDER HOSPEDAGEM
function rHosp(){
  var list=hospedagens.filter(function(h){return fH==='todos'||h.status===fH;});
  var el=document.getElementById('hLst'),em=document.getElementById('hEm');
  document.getElementById('hRc').innerHTML='<strong>'+list.length+'</strong> hospedagem'+(list.length!==1?'s':'')+' encontrada'+(list.length!==1?'s':'');
  if(!list.length){el.innerHTML='';em.classList.add('v');return;}em.classList.remove('v');
  var h='';for(var i=0;i<list.length;i++){var ho=list[i],n=ho.checkin&&ho.checkout?Math.round((new Date(ho.checkout)-new Date(ho.checkin))/86400000):0;h+='<div class="card" style="animation-delay:'+(i*28)+'ms" onclick="opDH('+ho.id+')">'+'<div class="av" style="background:#E8D5F5">🛏️</div>'+'<div class="ci"><div class="cnr"><span class="cn">'+nCao(ho.alunoId)+'</span></div><div class="cm">'+nCli(ho.clienteId)+'</div><div class="cm">'+fmtD(ho.checkin)+' → '+fmtD(ho.checkout)+' · <strong>'+n+' noite'+(n!==1?'s':'')+'</strong></div></div>'+'<div class="cr"><span class="badge '+ho.status+'">'+slbl(ho.status)+'</span>'+(ho.valor?'<span class="badge '+ho.statusPgto+'">'+fmtR(ho.valor)+'</span>':'')+'</div></div>';}
  el.innerHTML=h;
}

// RENDER AGENDA
function rAgenda(){
  var DIAS_PT=['D','S','T','Q','Q','S','S'];
  document.getElementById('calM').textContent=MES[calM2]+' '+calY;
  var apd={};
  for(var i=0;i<alunos.length;i++) for(var j=0;j<alunos[i].aulas.length;j++){var au=alunos[i].aulas[j];var p=au.data.split('-');if(parseInt(p[0])===calY&&parseInt(p[1])-1===calM2){if(!apd[au.data])apd[au.data]=[];apd[au.data].push({data:au.data,hora:au.hora,feita:au.feita,label:alunos[i].nome,sub:alunos[i].raca,tipo:'aula'});}}
  for(var i=0;i<hospedagens.length;i++){var ho=hospedagens[i];if(ho.checkin){var p=ho.checkin.split('-');if(parseInt(p[0])===calY&&parseInt(p[1])-1===calM2){if(!apd[ho.checkin])apd[ho.checkin]=[];apd[ho.checkin].push({data:ho.checkin,hora:'',feita:false,label:'🛏️ '+nCao(ho.alunoId)+' check-in',sub:nCli(ho.clienteId),tipo:'hosp'});}}if(ho.checkout){var p=ho.checkout.split('-');if(parseInt(p[0])===calY&&parseInt(p[1])-1===calM2){if(!apd[ho.checkout])apd[ho.checkout]=[];apd[ho.checkout].push({data:ho.checkout,hora:'',feita:false,label:'🛏️ '+nCao(ho.alunoId)+' check-out',sub:nCli(ho.clienteId),tipo:'hosp'});}}}
  var dw='';for(var i=0;i<DIAS_PT.length;i++) dw+='<div style="text-align:center;font-size:.55rem;font-weight:700;color:var(--mu);padding:1px 0">'+DIAS_PT[i]+'</div>';
  document.getElementById('calDW').innerHTML=dw;
  var pr=new Date(calY,calM2,1),ud=new Date(calY,calM2+1,0).getDate(),idw=pr.getDay();
  var gh='';for(var i=0;i<idw;i++) gh+='<div></div>';
  for(var d=1;d<=ud;d++){var m2=calM2+1,ds=calY+'-'+(m2<10?'0':'')+m2+'-'+(d<10?'0':'')+d;var aDs=apd[ds]||[];var cls='cdy';if(ds===hoje)cls+=' tod';if(ds===calDS)cls+=' sel';if(aDs.length){var tA=aDs.some(function(x){return !x.feita&&x.data<hoje;}),tP=aDs.some(function(x){return !x.feita&&x.data>=hoje;}),tD=aDs.some(function(x){return x.feita;});if(tA)cls+=' ha';else if(tP)cls+=' hp';else if(tD)cls+=' hd';}gh+='<div class="'+cls+'" onclick="selDia(\''+ds+'\')">'+d+'</div>';}
  document.getElementById('calG').innerHTML=gh;
  rCalDL(apd);
}
function selDia(ds){calDS=(calDS===ds?null:ds);rAgenda();}
function mvCal(dir){calM2+=dir;if(calM2>11){calM2=0;calY++;}if(calM2<0){calM2=11;calY--;}rAgenda();}
function rCalDL(apd){
  var el=document.getElementById('calDL');
  var todas=[];var dias=Object.keys(apd||{}).sort();
  for(var d=0;d<dias.length;d++) if(!calDS||dias[d]===calDS){var arr=apd[dias[d]];for(var j=0;j<arr.length;j++) todas.push(arr[j]);}
  todas.sort(function(a,b){return a.data!==b.data?a.data.localeCompare(b.data):(a.hora||'').localeCompare(b.hora||'');});
  if(!todas.length){el.innerHTML='<div style="text-align:center;color:var(--mu);font-size:.75rem;padding:20px 0">Nenhum evento '+(calDS?'neste dia':'neste mês')+'</div>';return;}
  var stC={done:'var(--gn)',pend:'var(--or)',atras:'var(--rd)'};var stL={done:'Realizada',pend:'Agendada',atras:'Atrasada'};
  var grp={};for(var i=0;i<todas.length;i++){var au=todas[i];if(!grp[au.data])grp[au.data]=[];grp[au.data].push(au);}
  var h='';var gd=Object.keys(grp).sort();
  for(var d=0;d<gd.length;d++){h+='<div style="font-size:.62rem;font-weight:700;letter-spacing:.1em;text-transform:uppercase;color:var(--mu);margin:9px 0 5px">'+fmtD(gd[d])+' · '+fmtDow(gd[d])+'</div>';var arr=grp[gd[d]];for(var j=0;j<arr.length;j++){var au=arr[j];var st=auSt(au);var cor=au.tipo==='hosp'?'var(--pu)':stC[st];var lbl=au.tipo==='hosp'?'Hosped.':stL[st];h+='<div class="card" style="cursor:default;margin-bottom:5px"><div class="av" style="background:#EDE0F7;font-size:16px">'+(au.tipo==='hosp'?'🛏️':'🐕')+'</div><div class="ci"><div class="cnr"><span class="cn">'+au.label+'</span>'+(au.hora?'<span class="cs">'+au.hora+'</span>':'')+'</div><div class="cm">'+au.sub+'</div></div><div class="cr"><span class="badge" style="background:'+cor+'18;color:'+cor+'">'+lbl+'</span></div></div>';}}
  el.innerHTML=h;
}

// RENDER FINANCEIRO
function rFinanceiro(){
  var m2=finM+1;
  var mesStr=finY+'-'+(m2<10?'0':'')+m2;
  document.getElementById('finMesLabel').textContent=MES[finM]+' '+finY;

  // Montar lista de receitas do mês
  var receitas=[];
  for(var i=0;i<alunos.length;i++){
    var a=alunos[i];
    if(a.pacote&&a.pacote.valor&&a.pacote.inicio&&a.pacote.inicio.slice(0,7)===mesStr)
      receitas.push({tipo:'receita',icone:'🐕',label:a.nome+' — Adestramento',sub:nCli(a.clienteId)+' · '+a.pacote.formaPgto,valor:a.pacote.valor,status:a.pacote.statusPgto,data:a.pacote.inicio,id:'a'+a.id});
  }
  for(var i=0;i<creches.length;i++){
    var cr=creches[i];
    if(cr.valor&&cr.inicio&&cr.inicio.slice(0,7)===mesStr)
      receitas.push({tipo:'receita',icone:'🏠',label:nCao(cr.alunoId)+' — Creche '+(cr.tipo==='mensal'?'Mensal':'Trimestral'),sub:nCli(cr.clienteId)+' · '+cr.formaPgto,valor:cr.valor,status:cr.statusPgto,data:cr.inicio,id:'cr'+cr.id});
  }
  for(var i=0;i<hospedagens.length;i++){
    var ho=hospedagens[i];
    if(ho.valor&&ho.checkin&&ho.checkin.slice(0,7)===mesStr)
      receitas.push({tipo:'receita',icone:'🛏️',label:nCao(ho.alunoId)+' — Hospedagem',sub:nCli(ho.clienteId)+' · '+ho.formaPgto,valor:ho.valor,status:ho.statusPgto,data:ho.checkin,id:'h'+ho.id});
  }

  // Despesas do mês
  var despsM=despesas.filter(function(d){return d.data&&d.data.slice(0,7)===mesStr;});
  var despLst=despsM.map(function(d){return{tipo:'despesa',icone:'📦',label:d.descricao,sub:d.categoria+' · '+(d.formaPgto||'—'),valor:d.valor,data:d.data,id:'d'+d.id,realId:d.id};});

  // Totais
  var totRec=receitas.reduce(function(s,r){return s+(parseFloat(r.valor)||0);},0);
  var totDesp=despsM.reduce(function(s,d){return s+(parseFloat(d.valor)||0);},0);
  var saldo=totRec-totDesp;

  document.getElementById('finSummary').innerHTML=
    '<div class="fin-card"><div class="fin-card-label">Receitas</div><div class="fin-card-val rec">'+fmtR(totRec)+'</div></div>'
    +'<div class="fin-card"><div class="fin-card-label">Despesas</div><div class="fin-card-val desp">'+fmtR(totDesp)+'</div></div>'
    +'<div class="fin-card"><div class="fin-card-label">Saldo</div><div class="fin-card-val saldo '+(saldo>=0?'pos':'neg')+'">'+fmtR(saldo)+'</div></div>';

  // Lista
  var todos=receitas.concat(despLst);
  todos.sort(function(a,b){return b.data.localeCompare(a.data);});
  var filtrado=todos.filter(function(x){return fFin==='todos'||x.tipo===fFin;});

  var el=document.getElementById('finLst'),em=document.getElementById('finEm');
  if(!filtrado.length){el.innerHTML='';em.classList.add('v');return;}em.classList.remove('v');
  var h='';
  for(var i=0;i<filtrado.length;i++){
    var it=filtrado[i];
    var isD=it.tipo==='despesa';
    var delBtn=isD?('<button class="fin-del" onclick="exDesp('+it.realId+',event)">🗑</button>'):'';
    var statusTag=!isD?('<span class="badge '+it.status+'" style="font-size:.55rem;margin-top:3px">'+slbl(it.status)+'</span>'):'';
    h+='<div class="fin-item" style="animation-delay:'+(i*25)+'ms">'
      +'<div class="fin-icon" style="background:'+(isD?'rgba(192,57,43,.1)':'rgba(46,139,87,.1)')+'">'+it.icone+'</div>'
      +'<div class="fin-info"><div class="fin-desc">'+it.label+'</div><div class="fin-sub">'+it.sub+'</div>'+statusTag+'</div>'
      +'<div class="fin-val '+(isD?'desp':'rec')+'">'+(isD?'- ':'')+fmtR(it.valor)+'</div>'
      +delBtn+'</div>';
  }
  el.innerHTML=h;
}
function mvFin(dir){finM+=dir;if(finM>11){finM=0;finY++;}if(finM<0){finM=11;finY--;}rFinanceiro();}

function renderAll(){rClientes();rAlunos();rCreche();rHosp();rAgenda();rFinanceiro();}

// ── CLIENTES ──────────────────────────────────────────────────────────
function abrirNC(){edC=null;document.getElementById('mClT').textContent='Novo Cliente';document.getElementById('bSvC').textContent='Cadastrar cliente';['cN','cS','cW','cE','cRu','cBa','cCi','cCe','cEs','cCo'].forEach(function(id){document.getElementById(id).value='';});openM('mCl');}
function abrirEC(id){edC=id;var c=null;for(var i=0;i<clientes.length;i++) if(clientes[i].id===id){c=clientes[i];break;}if(!c) return;document.getElementById('mClT').textContent='Editar Cliente';document.getElementById('bSvC').textContent='Salvar alterações';document.getElementById('cN').value=c.nome||'';document.getElementById('cS').value=c.sobrenome||'';document.getElementById('cW').value=c.whatsapp||'';document.getElementById('cE').value=c.email||'';document.getElementById('cRu').value=c.rua||'';document.getElementById('cBa').value=c.bairro||'';document.getElementById('cCi').value=c.cidade||'';document.getElementById('cCe').value=c.cep||'';document.getElementById('cEs').value=c.estado||'';document.getElementById('cCo').value=c.comp||'';closeM('mDC');openM('mCl');}
function svCliente(){var nome=document.getElementById('cN').value.trim();if(!nome){alert('Informe o nome.');return;}var obj={nome:nome,sobrenome:document.getElementById('cS').value.trim(),whatsapp:document.getElementById('cW').value.trim(),email:document.getElementById('cE').value.trim(),rua:document.getElementById('cRu').value.trim(),bairro:document.getElementById('cBa').value.trim(),cidade:document.getElementById('cCi').value.trim(),cep:document.getElementById('cCe').value.trim(),estado:document.getElementById('cEs').value.trim(),comp:document.getElementById('cCo').value.trim()};if(edC!==null){for(var i=0;i<clientes.length;i++) if(clientes[i].id===edC){obj.id=edC;clientes[i]=obj;break;}}else{obj.id=nxC++;clientes.push(obj);}closeM('mCl');renderAll();}
function exCliente(id){pedirConf('Excluir cliente','Todos os cães e registros vinculados serão removidos.',function(){var nC=[],nA=[],nCr=[],nH=[];for(var i=0;i<clientes.length;i++) if(clientes[i].id!==id) nC.push(clientes[i]);for(var i=0;i<alunos.length;i++) if(alunos[i].clienteId!==id) nA.push(alunos[i]);for(var i=0;i<creches.length;i++) if(creches[i].clienteId!==id) nCr.push(creches[i]);for(var i=0;i<hospedagens.length;i++) if(hospedagens[i].clienteId!==id) nH.push(hospedagens[i]);clientes=nC;alunos=nA;creches=nCr;hospedagens=nH;closeM('mDC');renderAll();});}
function opDC(id){var c=null;for(var i=0;i<clientes.length;i++) if(clientes[i].id===id){c=clientes[i];break;}if(!c) return;var sa=alunos.filter(function(a){return a.clienteId===id;});var saH='';if(sa.length){saH='<div class="scs"><div class="sct">Cães</div>';for(var i=0;i<sa.length;i++) saH+='<div class="arow" style="cursor:pointer" onclick="closeM(\'mDC\');opDA('+sa[i].id+')"><div class="anum pend">🐕</div><div class="ainf"><div class="adt">'+sa[i].nome+'</div><div class="asb">'+sa[i].raca+' · '+slbl(sa[i].status)+'</div></div><span style="color:var(--dm)">›</span></div>';saH+='</div>';}else saH='<p style="text-align:center;color:var(--mu);font-size:.75rem;margin-bottom:10px">Nenhum cão cadastrado</p>';var waH=c.whatsapp?('<button class="bwa" onclick="window.open(\'https://wa.me/55'+c.whatsapp.replace(/\D/g,'')+'\',\'_blank\')">📱 WhatsApp</button>'):'';document.getElementById('dCB').innerHTML='<div class="dav" style="background:#EDE0F7;font-size:26px">👤</div><div class="dn">'+c.nome+' '+c.sobrenome+'</div><div class="dm">'+(c.bairro&&c.cidade?c.bairro+', '+c.cidade:c.cidade||'—')+'</div><div class="dact"><button class="bedt" onclick="abrirEC('+c.id+')">✏️ Editar</button><button class="bdel" onclick="exCliente('+c.id+')">🗑 Excluir</button></div><div class="dgd"><div class="df"><div class="dfl">WhatsApp</div><div class="dfv" style="font-size:.73rem">'+(c.whatsapp||'—')+'</div></div><div class="df"><div class="dfl">E-mail</div><div class="dfv" style="font-size:.7rem;word-break:break-all">'+(c.email||'—')+'</div></div>'+(endStr(c)!=='—'?'<div class="df" style="grid-column:1/-1"><div class="dfl">Endereço</div><div class="dfv" style="font-weight:400;font-size:.72rem;line-height:1.5">'+endStr(c)+'</div></div>':'')+'</div>'+saH+waH;openM('mDC');}

// ── ALUNOS ────────────────────────────────────────────────────────────
function abrirNA(){edA=null;document.getElementById('mAlT').textContent='Novo Aluno';document.getElementById('bSvA').textContent='Cadastrar aluno';fillCl('aCl',null);['aN','aR','aI','aOb','aPk','aVal','aPgto'].forEach(function(id){document.getElementById(id).value='';});document.getElementById('aSt').value='avaliacao';document.getElementById('aSt2').value=hoje;document.getElementById('aStPgto').value='pendente';clearSlots('aDHSlots');resetPgtoGrid('aPgtoGrid','aPgto');openM('mAl');}
function abrirEA(id){edA=id;var a=null;for(var i=0;i<alunos.length;i++) if(alunos[i].id===id){a=alunos[i];break;}if(!a) return;document.getElementById('mAlT').textContent='Editar Aluno';document.getElementById('bSvA').textContent='Salvar alterações';fillCl('aCl',a.clienteId);document.getElementById('aN').value=a.nome||'';document.getElementById('aR').value=a.raca||'';document.getElementById('aI').value=a.idade||'';document.getElementById('aOb').value=a.obs||'';document.getElementById('aSt').value=a.status||'avaliacao';document.getElementById('aPk').value=a.pacote?a.pacote.total:'';document.getElementById('aSt2').value=a.pacote?a.pacote.inicio:hoje;document.getElementById('aVal').value=a.pacote?a.pacote.valor||'':'';document.getElementById('aStPgto').value=a.pacote?a.pacote.statusPgto||'pendente':'pendente';preselPgto('aPgtoGrid','aPgto',a.pacote?a.pacote.formaPgto:null);loadSlots('aDHSlots',a.pacote?a.pacote.diasHoras:null);var sv=a.servicos||[];document.getElementById('svcAdest').checked=sv.indexOf('adestramento')>=0;document.getElementById('svcCreche').checked=sv.indexOf('creche')>=0;document.getElementById('svcHosp').checked=sv.indexOf('hospedagem')>=0;closeM('mDA');openM('mAl');}
function svAluno(){var nome=document.getElementById('aN').value.trim();var cliId=parseInt(document.getElementById('aCl').value);if(!nome){alert('Informe o nome do cão.');return;}if(!cliId){alert('Selecione o tutor.');return;}var pk=parseInt(document.getElementById('aPk').value)||0;var ini=document.getElementById('aSt2').value;var dh=getSlots('aDHSlots');var val=parseFloat(document.getElementById('aVal').value)||0;var pgto=document.getElementById('aPgto').value.trim();var stPgto=document.getElementById('aStPgto').value;var pacote=pk?{total:pk,diasHoras:dh,inicio:ini,valor:val,formaPgto:pgto,statusPgto:stPgto}:null;var svcs=[];if(document.getElementById('svcAdest').checked) svcs.push('adestramento');if(document.getElementById('svcCreche').checked) svcs.push('creche');if(document.getElementById('svcHosp').checked) svcs.push('hospedagem');var obj={nome:nome,raca:document.getElementById('aR').value.trim()||'Srd',idade:document.getElementById('aI').value.trim()||'—',status:document.getElementById('aSt').value,clienteId:cliId,obs:document.getElementById('aOb').value.trim(),servicos:svcs,pacote:pacote};if(edA!==null){
  for(var i=0;i<alunos.length;i++) if(alunos[i].id===edA){
    obj.id=edA;
    var feitas=alunos[i].aulas.filter(function(x){return x.feita;});
    var pendNec=Math.max(0,pk-feitas.length);
    var novasPend=gAulas(ini,dh,pendNec);
    obj.aulas=feitas.concat(novasPend);
    obj.aulas.sort(function(x,y){return x.data.localeCompare(y.data);});
    alunos[i]=obj;break;
  }
} else {
  obj.id=nxA++;obj.aulas=gAulas(ini,dh,pk);alunos.push(obj);
}closeM('mAl');renderAll();}
function exAluno(id){pedirConf('Excluir aluno','Este aluno e todas as suas aulas serão removidos.',function(){var nA=[];for(var i=0;i<alunos.length;i++) if(alunos[i].id!==id) nA.push(alunos[i]);alunos=nA;closeM('mDA');renderAll();});}
function opDA(id){auAlvo=id;rdDA(id);openM('mDA');}
function rdDA(id){
  var a=null;for(var i=0;i<alunos.length;i++) if(alunos[i].id===id){a=alunos[i];break;}if(!a) return;
  var ft=a.aulas.filter(function(x){return x.feita;}).length;
  var tot=a.pacote?a.pacote.total:0,res=tot-ft,pct=tot>0?Math.round(ft/tot*100):0;
  var at=a.aulas.filter(function(x){return !x.feita&&x.data<hoje;}).length;
  var cli=null;for(var i=0;i<clientes.length;i++) if(clientes[i].id===a.clienteId){cli=clientes[i];break;}
  var srt=a.aulas.slice().sort(function(x,y){var ox=x.feita?1:0,oy=y.feita?1:0;return ox!==oy?ox-oy:x.data.localeCompare(y.data);});
  var pgtoH=a.pacote&&a.pacote.valor?('<div class="df"><div class="dfl">Valor</div><div class="dfv">'+fmtR(a.pacote.valor)+'</div></div><div class="df"><div class="dfl">Pagamento</div><div class="dfv" style="color:'+(a.pacote.statusPgto==='pago'?'var(--gn)':a.pacote.statusPgto==='parcial'?'var(--co)':'var(--or)')+'">'+slbl(a.pacote.statusPgto)+'</div></div>'+(a.pacote.formaPgto?'<div class="df"><div class="dfl">Forma</div><div class="dfv" style="font-size:.75rem">'+a.pacote.formaPgto+'</div></div>':'')):'';
  var pbH=a.pacote?('<div class="pbox"><div class="pbhd"><div><div class="pbt">📦 Pacote</div><div class="pbd">'+dhStr(a.pacote.diasHoras)+'</div></div><div class="pbn">'+Math.max(res,0)+'<span> rest.</span></div></div><div class="pbr"><div class="pbf" style="width:'+Math.min(pct,100)+'%;background:'+pCol(pct)+'"></div></div><div class="pbl">'+ft+' de '+tot+' realizadas'+(at>0?' · <span style="color:var(--rd)">'+at+' atrasada'+(at>1?'s':'')+'</span>':'')+'</div></div>'):'<p style="text-align:center;color:var(--mu);font-size:.75rem;margin-bottom:10px">Sem pacote definido</p>';
  var schH='';if(a.aulas.length){schH='<div class="scs"><div class="sct">Cronograma — '+a.aulas.length+' aula'+(a.aulas.length>1?'s':'')+'</div>';for(var i=0;i<srt.length;i++){var au=srt[i],idx=a.aulas.indexOf(au),st=auSt(au);schH+='<div class="arow"><div class="anum '+st+'">'+(idx+1)+'</div><div class="ainf"><div class="adt">'+fmtD(au.data)+(au.hora?' · '+au.hora:'')+'</div><div class="asb">'+fmtDow(au.data)+(au.obs?' · '+au.obs:'')+'</div></div><button class="achk'+(au.feita?' done':'')+'" onclick="tgAula('+a.id+','+idx+',event)">'+(au.feita?'✓':'○')+'</button><button class="adl" onclick="dlAula('+a.id+','+idx+',event)">🗑</button></div>';}schH+='</div>';}
  var waH=cli&&cli.whatsapp?('<button class="bwa" onclick="window.open(\'https://wa.me/55'+cli.whatsapp.replace(/\D/g,'')+'\',\'_blank\')">📱 WhatsApp do tutor</button>'):'';
  document.getElementById('dAB').innerHTML='<div class="dav" style="background:'+AVBG[(a.id-1)%AVBG.length]+';font-size:26px">🐕</div><div class="dn">'+a.nome+'</div><div class="dm">'+a.raca+' · '+a.idade+'</div><div class="dact"><button class="bedt" onclick="abrirEA('+a.id+')">✏️ Editar</button><button class="bdel" onclick="exAluno('+a.id+')">🗑 Excluir</button></div><div class="dgd"><div class="df"><div class="dfl">Cliente</div><div class="dfv">'+nCli(a.clienteId)+'</div></div><div class="df"><div class="dfl">Status</div><div class="dfv" style="color:'+(a.status==='ativo'?'var(--gn)':a.status==='concluido'?'var(--co)':'var(--or)')+'">'+slbl(a.status)+'</div></div>'+(a.obs?'<div class="df" style="grid-column:1/-1"><div class="dfl">Obs</div><div class="dfv" style="font-weight:400;font-size:.75rem">'+a.obs+'</div></div>':'')+pgtoH+'</div>'+pbH+(a.pacote?'<button class="bsch" onclick="abrirES('+a.id+')">✏️ Editar dias e horários</button>':'')+'<button class="baul" onclick="abrirAv('+a.id+')">+ Adicionar aula avulsa</button>'+schH+waH+'<div style="height:8px"></div>';
}
function tgAula(aId,idx,e){e.stopPropagation();for(var i=0;i<alunos.length;i++) if(alunos[i].id===aId){alunos[i].aulas[idx].feita=!alunos[i].aulas[idx].feita;break;}rdDA(aId);renderAll();}
function dlAula(aId,idx,e){e.stopPropagation();var aid=aId,ix=idx;pedirConf('Remover aula','Esta aula será removida do cronograma.',function(){for(var i=0;i<alunos.length;i++) if(alunos[i].id===aid){alunos[i].aulas.splice(ix,1);break;}rdDA(aid);renderAll();});}
function abrirAv(id){auAlvo=id;document.getElementById('avD').value=hoje;document.getElementById('avH').value='09:00';document.getElementById('avO').value='';openM('mAv');}
function svAvulsa(){var d=document.getElementById('avD').value;if(!d){alert('Selecione a data.');return;}for(var i=0;i<alunos.length;i++) if(alunos[i].id===auAlvo){alunos[i].aulas.push({data:d,hora:document.getElementById('avH').value,feita:false,obs:document.getElementById('avO').value.trim()});break;}closeM('mAv');rdDA(auAlvo);renderAll();}
function abrirES(id){auAlvo=id;var a=null;for(var i=0;i<alunos.length;i++) if(alunos[i].id===id){a=alunos[i];break;}if(!a||!a.pacote) return;loadSlots('eDHSlots',a.pacote.diasHoras);document.getElementById('eIni').value=hoje;openM('mES');}
function svEditSch(){var a=null;for(var i=0;i<alunos.length;i++) if(alunos[i].id===auAlvo){a=alunos[i];break;}if(!a) return;var nd=getSlots('eDHSlots');var ini=document.getElementById('eIni').value||hoje;if(!nd.length){alert('Adicione pelo menos 1 dia.');return;}var ft=a.aulas.filter(function(x){return x.feita;});var nv=gAulas(ini,nd,a.aulas.filter(function(x){return !x.feita;}).length);a.aulas=ft.concat(nv);a.aulas.sort(function(x,y){return x.data.localeCompare(y.data);});if(a.pacote){a.pacote.diasHoras=nd;a.pacote.inicio=ini;a.pacote.total=a.aulas.length;}closeM('mES');rdDA(auAlvo);renderAll();}

// ── CRECHE ────────────────────────────────────────────────────────────
function abrirNCr(){edCr=null;crTipo='mensal';document.getElementById('mCrT').textContent='Nova Creche';document.getElementById('bSvCr').textContent='Cadastrar pacote';fillCl('crCl',null);fillCao('crAl',null,null,'creche');document.querySelectorAll('#crDG .tpb.sel').forEach(function(b){b.classList.remove('sel');});selTipo('mensal');document.getElementById('crEn').value='07:00';document.getElementById('crSa').value='18:00';document.getElementById('crIn').value=hoje;document.getElementById('crTr').checked=false;document.getElementById('crOb').value='';document.getElementById('crVal').value='';document.getElementById('crStPgto').value='pendente';resetPgtoGrid('crPgtoGrid','crPgto');openM('mCr');}
function svCreche(){var cliId=parseInt(document.getElementById('crCl').value)||0;var aloId=parseInt(document.getElementById('crAl').value);if(!aloId){alert('Selecione o cão.');return;}var dias=[];document.querySelectorAll('#crDG .tpb.sel').forEach(function(b){dias.push(parseInt(b.dataset.d));});if(!dias.length){alert('Selecione pelo menos 1 dia.');return;}var obj={clienteId:cliId,alunoId:aloId,tipo:crTipo,dias:dias,entrada:document.getElementById('crEn').value,saida:document.getElementById('crSa').value,inicio:document.getElementById('crIn').value,transporte:document.getElementById('crTr').checked,obs:document.getElementById('crOb').value.trim(),status:'ativa',valor:parseFloat(document.getElementById('crVal').value)||0,formaPgto:document.getElementById('crPgto').value.trim(),statusPgto:document.getElementById('crStPgto').value};if(edCr!==null){for(var i=0;i<creches.length;i++) if(creches[i].id===edCr){obj.id=edCr;creches[i]=obj;break;}}else{obj.id=nxCr++;creches.push(obj);}closeM('mCr');renderAll();}
function opDCr(id){var cr=null;for(var i=0;i<creches.length;i++) if(creches[i].id===id){cr=creches[i];break;}if(!cr) return;var cli=null;for(var i=0;i<clientes.length;i++) if(clientes[i].id===cr.clienteId){cli=clientes[i];break;}var dias=cr.dias.map(function(d){return DPT[d];}).join(', ');var pgtoH=cr.valor?('<div class="df"><div class="dfl">Valor</div><div class="dfv">'+fmtR(cr.valor)+'</div></div><div class="df"><div class="dfl">Pagamento</div><div class="dfv" style="color:'+(cr.statusPgto==='pago'?'var(--gn)':cr.statusPgto==='parcial'?'var(--co)':'var(--or)')+'">'+slbl(cr.statusPgto)+'</div></div>'+(cr.formaPgto?'<div class="df"><div class="dfl">Forma</div><div class="dfv" style="font-size:.73rem">'+cr.formaPgto+'</div></div>':'')):'';document.getElementById('dCrB').innerHTML='<div class="dav" style="background:#D5EAF7">🏠</div><div class="dn">'+nCao(cr.alunoId)+'</div><div class="dm">'+(cr.tipo==='mensal'?'Mensal':'Trimestral')+' · '+nCli(cr.clienteId)+'</div><div class="dact"><button class="bdel" style="flex:1" onclick="exCreche('+cr.id+')">🗑 Encerrar pacote</button></div><div class="dgd"><div class="df"><div class="dfl">Dias</div><div class="dfv" style="font-size:.73rem">'+dias+'</div></div><div class="df"><div class="dfl">Horário</div><div class="dfv">'+cr.entrada+' – '+cr.saida+'</div></div><div class="df"><div class="dfl">Transporte</div><div class="dfv">'+(cr.transporte?'✅ Sim':'❌ Não')+'</div></div><div class="df"><div class="dfl">Início</div><div class="dfv">'+fmtD(cr.inicio)+'</div></div>'+pgtoH+(cr.obs?'<div class="df" style="grid-column:1/-1"><div class="dfl">Obs</div><div class="dfv" style="font-weight:400;font-size:.73rem">'+cr.obs+'</div></div>':'')+'</div>'+(cli&&cli.whatsapp?'<button class="bwa" onclick="window.open(\'https://wa.me/55'+cli.whatsapp.replace(/\D/g,'')+'\',\'_blank\')">📱 WhatsApp</button>':'');openM('mDCr');}
function exCreche(id){pedirConf('Encerrar creche','Este pacote de creche será removido.',function(){var nC=[];for(var i=0;i<creches.length;i++) if(creches[i].id!==id) nC.push(creches[i]);creches=nC;closeM('mDCr');renderAll();});}

// ── HOSPEDAGEM ────────────────────────────────────────────────────────
function abrirNH(){edH=null;document.getElementById('mHoT').textContent='Nova Hospedagem';document.getElementById('bSvH').textContent='Cadastrar hospedagem';fillCl('hoCl',null);fillCao('hoAl',null,null,'hospedagem');document.getElementById('hoCI').value=hoje;document.getElementById('hoCO').value='';document.getElementById('hoSt').value='agendado';document.getElementById('hoOb').value='';document.getElementById('hoVal').value='';document.getElementById('hoStPgto').value='pendente';resetPgtoGrid('hoPgtoGrid','hoPgto');openM('mHo');}
function svHosp(){var cliId=parseInt(document.getElementById('hoCl').value)||0;var aloId=parseInt(document.getElementById('hoAl').value);if(!aloId){alert('Selecione o cão.');return;}var ci=document.getElementById('hoCI').value;var co=document.getElementById('hoCO').value;if(!ci||!co){alert('Informe check-in e check-out.');return;}var obj={clienteId:cliId,alunoId:aloId,checkin:ci,checkout:co,status:document.getElementById('hoSt').value,obs:document.getElementById('hoOb').value.trim(),valor:parseFloat(document.getElementById('hoVal').value)||0,formaPgto:document.getElementById('hoPgto').value.trim(),statusPgto:document.getElementById('hoStPgto').value};if(edH!==null){for(var i=0;i<hospedagens.length;i++) if(hospedagens[i].id===edH){obj.id=edH;hospedagens[i]=obj;break;}}else{obj.id=nxH++;hospedagens.push(obj);}closeM('mHo');renderAll();}
function opDH(id){var ho=null;for(var i=0;i<hospedagens.length;i++) if(hospedagens[i].id===id){ho=hospedagens[i];break;}if(!ho) return;var cli=null;for(var i=0;i<clientes.length;i++) if(clientes[i].id===ho.clienteId){cli=clientes[i];break;}var n=ho.checkin&&ho.checkout?Math.round((new Date(ho.checkout)-new Date(ho.checkin))/86400000):0;var pgtoH=ho.valor?('<div class="df"><div class="dfl">Valor</div><div class="dfv">'+fmtR(ho.valor)+'</div></div><div class="df"><div class="dfl">Pagamento</div><div class="dfv" style="color:'+(ho.statusPgto==='pago'?'var(--gn)':ho.statusPgto==='parcial'?'var(--co)':'var(--or)')+'">'+slbl(ho.statusPgto)+'</div></div>'+(ho.formaPgto?'<div class="df"><div class="dfl">Forma</div><div class="dfv" style="font-size:.73rem">'+ho.formaPgto+'</div></div>':'')):'';document.getElementById('dHB').innerHTML='<div class="dav" style="background:#E8D5F5">🛏️</div><div class="dn">'+nCao(ho.alunoId)+'</div><div class="dm">'+nCli(ho.clienteId)+'</div><div class="dact"><button class="bedt" onclick="abrirEH('+ho.id+')">✏️ Editar</button><button class="bdel" onclick="exHosp('+ho.id+')">🗑 Excluir</button></div><div class="dgd"><div class="df"><div class="dfl">Check-in</div><div class="dfv">'+fmtD(ho.checkin)+'</div></div><div class="df"><div class="dfl">Check-out</div><div class="dfv">'+fmtD(ho.checkout)+'</div></div><div class="df"><div class="dfl">Noites</div><div class="dfv">'+n+'</div></div><div class="df"><div class="dfl">Status</div><div class="dfv" style="color:'+(ho.status==='hospedado'?'var(--te)':ho.status==='concluido'?'var(--co)':'var(--pu)')+'">'+slbl(ho.status)+'</div></div>'+pgtoH+(ho.obs?'<div class="df" style="grid-column:1/-1"><div class="dfl">Obs</div><div class="dfv" style="font-weight:400;font-size:.73rem">'+ho.obs+'</div></div>':'')+'</div>'+(cli&&cli.whatsapp?'<button class="bwa" onclick="window.open(\'https://wa.me/55'+cli.whatsapp.replace(/\D/g,'')+'\',\'_blank\')">📱 WhatsApp</button>':'');openM('mDH');}
function abrirEH(id){edH=id;var ho=null;for(var i=0;i<hospedagens.length;i++) if(hospedagens[i].id===id){ho=hospedagens[i];break;}if(!ho) return;document.getElementById('mHoT').textContent='Editar Hospedagem';document.getElementById('bSvH').textContent='Salvar alterações';fillCl('hoCl',ho.clienteId);fillCao('hoAl',ho.clienteId,ho.alunoId);document.getElementById('hoCI').value=ho.checkin||'';document.getElementById('hoCO').value=ho.checkout||'';document.getElementById('hoSt').value=ho.status||'agendado';document.getElementById('hoOb').value=ho.obs||'';document.getElementById('hoVal').value=ho.valor||'';document.getElementById('hoStPgto').value=ho.statusPgto||'pendente';preselPgto('hoPgtoGrid','hoPgto',ho.formaPgto);closeM('mDH');openM('mHo');}
function exHosp(id){pedirConf('Excluir hospedagem','Esta reserva será removida.',function(){var nH=[];for(var i=0;i<hospedagens.length;i++) if(hospedagens[i].id!==id) nH.push(hospedagens[i]);hospedagens=nH;closeM('mDH');renderAll();});}

// ── DESPESAS ──────────────────────────────────────────────────────────
function abrirNDesp(){document.getElementById('mDespT').textContent='Nova Despesa';['dDesc','dVal','dObs','dPgto'].forEach(function(id){document.getElementById(id).value='';});document.getElementById('dData').value=hoje;document.getElementById('dCat').value='Alimentação';resetPgtoGrid('dPgtoGrid','dPgto');openM('mDesp');}
function svDesp(){var desc=document.getElementById('dDesc').value.trim();var val=parseFloat(document.getElementById('dVal').value)||0;if(!desc){alert('Informe a descrição.');return;}if(!val){alert('Informe o valor.');return;}var obj={id:nxD++,descricao:desc,valor:val,data:document.getElementById('dData').value||hoje,categoria:document.getElementById('dCat').value,formaPgto:document.getElementById('dPgto').value.trim(),obs:document.getElementById('dObs').value.trim()};despesas.push(obj);closeM('mDesp');rFinanceiro();}
function exDesp(id,e){e.stopPropagation();pedirConf('Excluir despesa','Esta despesa será removida.',function(){var nD=[];for(var i=0;i<despesas.length;i++) if(despesas[i].id!==id) nD.push(despesas[i]);despesas=nD;rFinanceiro();});}

renderAll();
</script>
</body>
</html>
